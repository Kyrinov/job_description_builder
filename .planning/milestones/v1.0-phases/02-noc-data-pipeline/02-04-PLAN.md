---
phase: 02-noc-data-pipeline
plan: 04
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - app/main.py
autonomous: true
requirements:
  - PIPE-05
must_haves:
  truths:
    - "app/main.py lifespan calls assert_noc_index_model(con, settings.ollama_embed_model) after create_schema"
    - "Starting the app with a mismatched OLLAMA_EMBED_MODEL causes a RuntimeError at startup"
    - "Starting the app with no index (fresh install) does NOT cause a startup error"
    - "All existing test_startup.py tests remain green after lifespan change"
  artifacts:
    - path: "app/main.py"
      provides: "PIPE-05 lifespan integration"
      contains: "assert_noc_index_model(con, settings.ollama_embed_model)"
    - path: "app/main.py"
      provides: "Updated import"
      contains: "from app.db import assert_noc_index_model, create_schema, get_connection"
  key_links:
    - from: "app/main.py lifespan"
      to: "app/db.py assert_noc_index_model"
      via: "direct function call with con and settings.ollama_embed_model"
      pattern: "assert_noc_index_model\\(con"
    - from: "app/main.py lifespan"
      to: "settings.ollama_embed_model"
      via: "passes configured embed model name to assert_noc_index_model"
      pattern: "settings\\.ollama_embed_model"
---

<objective>
Wire assert_noc_index_model() into the FastAPI lifespan startup sequence. This is a one-line
addition: call assert_noc_index_model(con, settings.ollama_embed_model) after create_schema(con)
and before con.close(). Update the import line. Run all startup tests to confirm the lifespan
behaves correctly for both the mismatch (raises) and fresh-install (passes silently) cases.

Purpose: PIPE-05 requires the app to refuse to start when the embedding model in the index
metadata mismatches the configured model. assert_noc_index_model() (implemented in Plan 02)
does the check — this plan wires it into the startup sequence.
Output: app/main.py with one new import and one new lifespan call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-noc-data-pipeline/02-RESEARCH.md
@.planning/phases/02-noc-data-pipeline/02-PATTERNS.md
@.planning/phases/02-noc-data-pipeline/02-02-SUMMARY.md

<interfaces>
<!-- Current app/main.py lifespan (from Phase 1 — read file before editing): -->
<!--
@asynccontextmanager
async def lifespan(app: FastAPI) -> AsyncGenerator[None, None]:
    # --- startup ---
    await assert_ollama_ready()
    con = get_connection(settings.db_path)
    create_schema(con)
    con.close()

    yield

    # --- shutdown ---
-->

<!-- Target state after this plan: -->
<!--
    con = get_connection(settings.db_path)
    create_schema(con)
    assert_noc_index_model(con, settings.ollama_embed_model)  # PIPE-05
    con.close()
-->

<!-- Import line to update (current): -->
<!-- from app.db import create_schema, get_connection -->
<!-- Target: -->
<!-- from app.db import assert_noc_index_model, create_schema, get_connection -->

<!-- app/db.py exports available after Plan 02: -->
<!-- assert_noc_index_model(con: sqlite3.Connection, configured_model: str) -> None -->
<!-- create_schema(con: sqlite3.Connection) -> None -->
<!-- get_connection(db_path: str) -> sqlite3.Connection -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire assert_noc_index_model into app/main.py lifespan</name>
  <files>app/main.py</files>
  <read_first>
    - app/main.py (read current state — make exactly the two changes described; do NOT restructure)
    - tests/test_noc_startup.py (confirms assert_noc_index_model behavior; no changes needed to test file)
    - tests/test_startup.py (Phase 1 tests that must remain green after this change)
    - .planning/phases/02-noc-data-pipeline/02-PATTERNS.md (app/main.py section — exact lifespan extension diff)
  </read_first>
  <behavior>
    - test_model_mismatch_raises_runtime_error: RuntimeError propagates through lifespan when assert_noc_index_model raises
    - test_missing_index_metadata_no_error: lifespan completes normally when no index_metadata row exists
    - test_startup_fails_ollama_unreachable: existing Phase 1 test still passes (lifespan change does not break Ollama check)
    - test_startup_fails_missing_model: existing Phase 1 test still passes
  </behavior>
  <action>
Make exactly two changes to `app/main.py`:

**Change 1 — Update import on line ~22:**

Current:
```python
from app.db import create_schema, get_connection
```

Replace with:
```python
from app.db import assert_noc_index_model, create_schema, get_connection
```

**Change 2 — Add one line in lifespan, between create_schema and con.close():**

Current lifespan startup section:
```python
    # --- startup ---
    await assert_ollama_ready()
    con = get_connection(settings.db_path)
    create_schema(con)
    con.close()
```

Target state:
```python
    # --- startup ---
    await assert_ollama_ready()
    con = get_connection(settings.db_path)
    create_schema(con)
    assert_noc_index_model(con, settings.ollama_embed_model)  # PIPE-05
    con.close()
```

That is the entire diff. Do not change anything else in main.py.

After editing, verify both the new startup test and all existing startup tests pass:
```bash
cd /home/charles/job_description_builder && python -m pytest tests/test_noc_startup.py tests/test_startup.py -x -q 2>&1
```
All tests must pass.

Then run the full test suite:
```bash
cd /home/charles/job_description_builder && python -m pytest tests/ -x -q 2>&1
```
  </action>
  <verify>
    <automated>cd /home/charles/job_description_builder && python -m pytest tests/test_noc_startup.py tests/test_startup.py -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `app/main.py` contains `from app.db import assert_noc_index_model, create_schema, get_connection`
    - `app/main.py` contains `assert_noc_index_model(con, settings.ollama_embed_model)` inside the lifespan function
    - `app/main.py` contains the comment `# PIPE-05` on the assert_noc_index_model call line
    - `grep -n "assert_noc_index_model" app/main.py` returns 2 lines (import + call)
    - `pytest tests/test_noc_startup.py tests/test_startup.py -x -q` exits 0 (all tests pass)
    - `pytest tests/ -x -q` exits 0 (full suite green)
  </acceptance_criteria>
  <done>app/main.py lifespan calls assert_noc_index_model; all startup tests pass; full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lifespan → assert_noc_index_model | Internal function call; no external input crosses here |
| settings.ollama_embed_model → assert_noc_index_model | Config value validated at Settings instantiation (T-1-02); safe to pass directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | `--db-path` path injection | mitigate | Handled in Plan 03 (scripts/ingest_noc.py); app/main.py uses settings.db_path which is already validated by T-1-01 in app/config.py |
| T-2-02 | Tampering | SQL via CSV content | mitigate | Handled in Plan 03 (parameterized queries); no new SQL in this plan |
| T-2-03 | Spoofing | Ollama model name in index_metadata | accept | assert_noc_index_model only compares string values; model name never executed as code or used in shell commands |
</threat_model>

<verification>
After Plan 04 completes:

1. `grep -n "assert_noc_index_model" app/main.py` returns exactly 2 lines (import + call)
2. `grep -n "settings.ollama_embed_model" app/main.py` returns the lifespan call line
3. `pytest tests/test_noc_startup.py -x -q` exits 0 — all 4 PIPE-05 startup tests pass
4. `pytest tests/test_startup.py -x -q` exits 0 — all Phase 1 startup tests unbroken
5. `pytest tests/ -x -q` exits 0 — full suite green
6. `python -c "from app.main import lifespan; print('OK')"` prints OK (import clean, assuming env vars set)
</verification>

<success_criteria>
- assert_noc_index_model imported from app.db in app/main.py
- assert_noc_index_model called inside lifespan after create_schema, before con.close()
- settings.ollama_embed_model passed as the configured_model argument
- All Phase 1 test_startup.py tests remain green
- All Phase 2 test_noc_startup.py tests pass
- Full pytest tests/ suite exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-noc-data-pipeline/02-04-SUMMARY.md`
</output>
