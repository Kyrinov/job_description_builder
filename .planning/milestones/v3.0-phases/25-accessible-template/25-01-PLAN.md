---
phase: 25-accessible-template
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - v2/backend/tests/test_export.py
autonomous: true
requirements: [ACC-02, ACC-03, ACC-04]
must_haves:
  truths:
    - "A RED test baseline exists asserting the Accessible-format export behavior before any implementation"
    - "Four fixture helpers exist covering the four JES shapes: EC (Effort+Conditions categories), point-rating-with-Effort (FB), point-rating-without-Effort (MT or SW-SCW), level-description (jes_scores empty)"
    - "A content-presence test asserts no literal '{{' or 'None' string leaks into rendered DOCX text"
  artifacts:
    - path: "v2/backend/tests/test_export.py"
      provides: "ACC-02/ACC-04 test cases + 4 JES-shape fixture helpers"
      contains: "content_presence"
  key_links:
    - from: "v2/backend/tests/test_export.py"
      to: "POST /api/wd/{id}/export/docx"
      via: "client.post then python-docx read-back of resp.content"
      pattern: "docx\\.Document\\(io\\.BytesIO"
---

<objective>
Establish the RED test baseline for Phase 25 Accessible Template before any implementation exists. This plan adds the test cases and fixture helpers that pin down the expected Accessible-format behavior (ACC-02 Effort/Working-Conditions fallback, ACC-04 content-presence). The existing 13 export tests MUST continue to pass; the NEW tests added here are expected to FAIL (RED) until Plans 02 and 03 land.

Purpose: Lock the behavioral contract first so Plans 02/03 implement against fixed expectations, not a moving target. This is the Nyquist Wave 0 step — the build script and export_service rewrite have no automated gate without these tests.
Output: Updated `v2/backend/tests/test_export.py` with 4 JES-shape fixture helpers and new ACC-02/ACC-04 test functions (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-accessible-template/25-RESEARCH.md
@.planning/phases/25-accessible-template/25-PATTERNS.md

<interfaces>
<!-- Existing fixture helper and test patterns the executor MUST mirror. Extracted from v2/backend/tests/test_export.py. -->

Existing helpers (lines 19-54):
```python
async def _create_wd(client) -> str:
    resp = await client.post(
        "/api/wd",
        json={"record": {"title": "Test Role"}, "answers": {}, "step_index": 1},
    )
    assert resp.status_code == 201
    return resp.json()["id"]

async def _create_wd_with_jes_scores(client) -> str:
    wd_id = await _create_wd(client)
    resp = await client.patch(
        f"/api/wd/{wd_id}",
        json={
            "confirmed_og": {"og_code": "EC", "og_name": "..."},
            "og_level": 4,
            "jes_total_points": 621,
            "jes_scores": [
                {"factor_name": "Decision Making", "degree": 3, "points": 150},
                {"factor_name": "Communication", "degree": 2, "points": 84},
            ],
            "duties": [{"id": "d1", "text": "...", "source": "noc",
                        "provenance_noc_code": "4163", "provenance_hash": "abc123",
                        "advisor": False}],
        },
    )
    assert resp.status_code == 200
    return wd_id
```

Existing passing structural test (lines 57-65) — keeps passing unchanged, asserts MIME + non-zero bytes only:
```python
async def test_export_wd_docx_returns_bytes(client, env_with_db):
    wd_id = await _create_wd_with_jes_scores(client)
    resp = await client.post(f"/api/wd/{wd_id}/export/docx")
    assert resp.status_code == 200
    assert resp.headers["content-type"].startswith(
        "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
    assert len(resp.content) > 0
```

Factor `category` ground truth (from v2/backend/app/data/constants.py EC_JES_ELEMENTS, lines 1367-1375) — note keys are "name"/"pts", names are sentence-case:
- "Physical effort" -> category "Effort"
- "Sensory effort" -> category "Effort"
- "Working conditions" -> category "Conditions"
- "Decision making", "Leadership & operational mgmt" -> "Responsibility"
- "Communication", etc. -> "Skill"

FB point-rating group factors (JES_FACTORS_BY_GROUP["FB"], lines ~1410-1419) carry Effort + Conditions categories ("Physical effort"/"Sensory effort" -> Effort; "Risk to health"/"Work environment" -> Conditions).
MT and SW-SCW point-rating groups have NO Effort/Conditions-category factors (Skill/Responsibility only).
Level-description groups (IT, AS, FI, NU, etc.) produce jes_scores: [] entirely.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add four JES-shape fixture helpers</name>
  <files>v2/backend/tests/test_export.py</files>
  <read_first>
    - v2/backend/tests/test_export.py (current fixtures + tests — see exact current state)
    - v2/backend/app/data/constants.py (lines 1366-1470 — EC_JES_ELEMENTS, JES_FACTORS_BY_GROUP for FB/MT to copy real factor names)
    - .planning/phases/25-accessible-template/25-RESEARCH.md (Wave 0 Gaps section + Code Examples)
  </read_first>
  <behavior>
    - `_create_wd_ec(client)` returns a wd_id with confirmed_og EC, og_level 4, and jes_scores containing factor_name "Physical effort" (degree 2, points 4), "Sensory effort" (degree 1, points 2), "Working conditions" (degree 3, points 12), plus one Skill factor — exercises the EC path where category is NOT on the persisted dict.
    - `_create_wd_point_rating_with_effort(client)` returns a wd_id with confirmed_og FB, og_level set, and jes_scores containing FB's real Effort factors ("Physical effort", "Sensory effort") and Conditions factors ("Risk to health", "Work environment") — exercises the point-rating path that DOES carry category.
    - `_create_wd_point_rating_no_effort(client)` returns a wd_id with confirmed_og MT (or SW with confirmed_sub_group "SCW"), jes_scores containing only Skill/Responsibility factors, no Effort/Conditions — exercises the placeholder-fallback branch.
    - `_create_wd_level_description(client)` returns a wd_id with confirmed_og AS, og_level set, jes_total_points set, and jes_scores: [] — exercises the empty-scores fallback branch.
    - All four helpers also PATCH at least one duty, client_service_results text, and quals (education/experience) so downstream content-presence tests have non-empty Part 2 data.
  </behavior>
  <action>
    Add four async fixture helper functions to v2/backend/tests/test_export.py, modeled exactly on the existing `_create_wd_with_jes_scores` helper (call `_create_wd(client)` then PATCH /api/wd/{id}).

    Use these exact factor_name strings (they MUST match constants.py so the category lookup in Plan 03 resolves):
    - EC fixture jes_scores: `{"factor_name": "Physical effort", "degree": 2, "points": 4}`, `{"factor_name": "Sensory effort", "degree": 1, "points": 2}`, `{"factor_name": "Working conditions", "degree": 3, "points": 12}`, `{"factor_name": "Communication", "degree": 2, "points": 25}`. Set confirmed_og `{"og_code": "EC", "og_name": "Economics and Social Science Services"}`, og_level 4, jes_total_points 43.
    - FB fixture jes_scores: `{"factor_name": "Physical effort", "degree": 2, "points": 5}`, `{"factor_name": "Sensory effort", "degree": 2, "points": 4}`, `{"factor_name": "Risk to health", "degree": 2, "points": 10}`, `{"factor_name": "Work environment", "degree": 1, "points": 2}`, `{"factor_name": "Knowledge", "degree": 3, "points": 50}`. confirmed_og `{"og_code": "FB", "og_name": "Border Services"}`, og_level set to a valid FB level, jes_total_points 71.
    - MT fixture jes_scores: only Skill/Responsibility factors (use `{"factor_name": "Knowledge", "degree": 3, "points": 50}`, `{"factor_name": "Decision making", "degree": 3, "points": 60}`). confirmed_og `{"og_code": "MT", "og_name": "Meteorology"}`, jes_total_points 110. NO Effort/Conditions factors.
    - AS (level-description) fixture: confirmed_og `{"og_code": "AS", "og_name": "Administrative Services"}`, og_level 4, jes_total_points 500, `"jes_scores": []`.

    In ALL four helpers, also include in the PATCH body:
    - `"duties": [{"id": "d1", "text": "Provides advice on policy matters.", "source": "noc", "provenance_noc_code": "4163", "provenance_hash": "abc123", "advisor": False}]`
    - `"record"` merge is not directly patchable via PATCH if the endpoint replaces; instead set client_service_results and quals via whatever the PATCH contract supports. FIRST read how `_create_wd` seeds `record` and how PATCH merges `record`/`qualification` (grep the wd router and WorkDescription model). If PATCH accepts a `record` dict, include `"record": {"title": "Test Role", "client_service_results": "Citizens receive timely, accurate policy guidance.", "quals": {"education": "Degree in economics.", "experience": "5 years policy analysis."}}`. If qualification is a top-level field, also PATCH `"qualification": {"education": "Degree in economics.", "experience": "5 years policy analysis."}`.

    Place all four helpers immediately after the existing `_create_wd_with_jes_scores` helper (after line 54). Do NOT modify the existing helpers.
  </action>
  <verify>
    <automated>cd v2/backend && python -m pytest tests/test_export.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_create_wd_ec\|_create_wd_point_rating_with_effort\|_create_wd_point_rating_no_effort\|_create_wd_level_description" v2/backend/tests/test_export.py` returns 4+ definition lines
    - `grep -c "Physical effort" v2/backend/tests/test_export.py` returns >= 2 (EC + FB fixtures)
    - `grep -n "client_service_results" v2/backend/tests/test_export.py` returns at least one hit
    - The existing 13 tests still pass: `cd v2/backend && python -m pytest tests/test_export.py -q` shows no NEW failures attributable to the helpers themselves (helpers are not called by failing tests yet)
  </acceptance_criteria>
  <done>Four fixture helpers exist using real constants.py factor names; existing tests unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ACC-02 Effort/Working-Conditions and ACC-04 content-presence tests (RED)</name>
  <files>v2/backend/tests/test_export.py</files>
  <read_first>
    - v2/backend/tests/test_export.py (after Task 1 — the new fixtures)
    - .planning/phases/25-accessible-template/25-RESEARCH.md (Code Examples: content-presence test pattern lines 270-282; ACC-02 expectations)
  </read_first>
  <behavior>
    - `test_accessible_effort_ec_populated`: EC export contains the Effort factor names "Physical effort"/"Sensory effort" somewhere in the rendered DOCX text AND "Working conditions" in the Working Conditions section — i.e. EC's effort/conditions are populated despite the persisted dict lacking the category key.
    - `test_accessible_effort_fb_populated`: FB export contains "Risk to health" and "Work environment" (Conditions) and "Physical effort"/"Sensory effort" (Effort) in rendered text.
    - `test_accessible_effort_no_factor_group_placeholder`: MT export contains the literal string "[To be completed by advisor]" in the Effort and Working Conditions sections (no effort/conditions factor data for MT).
    - `test_accessible_effort_level_description_placeholder`: AS export (jes_scores empty) contains "[To be completed by advisor]" in Effort and Working Conditions sections.
    - `test_accessible_content_presence`: For a fully-completed EC WD, the rendered DOCX (read back via python-docx) contains NO literal "{{" substring and NO bare "None" token — every non-placeholder variable resolved to a non-empty string.
    - `test_accessible_structure_headings`: The rendered DOCX paragraph text includes all 7 Part 2 subsection headings (Organizational context, Client service results, Key activities, Skills, Effort, Responsibilities, Working conditions) and the Part 1 / Part 2 markers.
  </behavior>
  <action>
    Add six new async test functions to v2/backend/tests/test_export.py after the existing tests. Each posts to `POST /api/wd/{wd_id}/export/docx` using the matching fixture helper from Task 1, then reads back `resp.content` via python-docx.

    Add module imports at top of file if not present: `import io` and `import docx`.

    Implement a small read-back helper at module level:
    ```python
    def _docx_text(content: bytes) -> str:
        d = docx.Document(io.BytesIO(content))
        parts = [p.text for p in d.paragraphs]
        for t in d.tables:
            for row in t.rows:
                parts.extend(c.text for c in row.cells)
        return "\n".join(parts)
    ```

    Test bodies:
    - `test_accessible_effort_ec_populated`: wd_id = await _create_wd_ec(client); resp = post export; text = _docx_text(resp.content); assert "Physical effort" in text and "Sensory effort" in text and "Working conditions" in text; assert "[To be completed by advisor]" NOT in the Effort region (acceptably: assert text.count("[To be completed by advisor]") does not cover Effort — simplest: assert "Physical effort" in text).
    - `test_accessible_effort_fb_populated`: assert "Risk to health" in text and "Work environment" in text.
    - `test_accessible_effort_no_factor_group_placeholder`: use _create_wd_point_rating_no_effort; assert "[To be completed by advisor]" in text.
    - `test_accessible_effort_level_description_placeholder`: use _create_wd_level_description; assert "[To be completed by advisor]" in text.
    - `test_accessible_content_presence`: use _create_wd_ec; assert "{{" not in text; assert "\nNone\n" not in ("\n"+text+"\n") (guard against str(None) leaking); assert "%}" not in text (no unrendered Jinja2 tag).
    - `test_accessible_structure_headings`: use _create_wd_ec; for heading in ["Organizational context", "Client service results", "Key activities", "Skills", "Effort", "Responsibilities", "Working conditions"]: assert heading in text. Also assert "Part 1" in text and "Part 2" in text.

    These tests are EXPECTED TO FAIL now (RED) because the Accessible template and rewritten _build_wd_context do not exist yet. That is correct for Wave 0. Do NOT skip them with pytest.mark.skip — they must visibly RED so Plans 02/03 turn them GREEN.
  </action>
  <verify>
    <automated>cd v2/backend && python -m pytest tests/test_export.py -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def test_accessible" v2/backend/tests/test_export.py` returns 6
    - `grep -n "def _docx_text" v2/backend/tests/test_export.py` returns one hit
    - `grep -n "To be completed by advisor" v2/backend/tests/test_export.py` returns at least 2 hits
    - Running the suite shows the 6 new `test_accessible_*` tests FAILING (RED) while the original 13 tests still PASS — confirm with `cd v2/backend && python -m pytest tests/test_export.py -q 2>&1 | tail -8` showing "6 failed, 13 passed" or equivalent
  </acceptance_criteria>
  <done>Six RED tests pin the Accessible-format contract; 13 prior tests still green.</done>
</task>

</tasks>

<verification>
- `cd v2/backend && python -m pytest tests/test_export.py -q` shows exactly the 6 new tests RED and all prior tests GREEN
- No test uses `pytest.mark.skip`
- Fixture helpers use exact factor_name strings from constants.py
</verification>

<success_criteria>
- 4 JES-shape fixture helpers exist (EC, FB, MT/SW-SCW, AS)
- 6 new RED tests cover ACC-02 (4 effort/conditions branches) and ACC-04 (content-presence) and structure
- Existing 13 export tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/25-accessible-template/25-01-SUMMARY.md`
</output>
