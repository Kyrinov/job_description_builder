---
phase: 13-frontend-spa-shell
plan: 02
type: execute
wave: 1
depends_on:
  - "13-01"
files_modified:
  - v2/frontend/index.html
  - v2/frontend/src/main.jsx
  - v2/frontend/src/styles.css
  - v2/frontend/src/data.jsx
  - v2/frontend/src/components.jsx
autonomous: true
requirements:
  - FE-01
  - FE-03

must_haves:
  truths:
    - "data.jsx exports I, STEPS, PHASES, DRF, WORK_TYPES, DUTY_SUGGESTIONS, QUAL_DEFAULT, EC_ELEMENTS, computeClassification, refineDuty, ecFactors as named ES module exports"
    - "components.jsx exports Icon, Check, StepInput, initialAnswer, answerValid as named ES module exports"
    - "styles.css contains all three brand font CSS custom properties"
    - "index.html has the Google Fonts <link> for Hanken Grotesk (variable range), Spectral, Spline Sans Mono"
    - "main.jsx imports app.jsx (lowercase) and styles.css"
    - "npm run build produces no errors"
  artifacts:
    - path: "v2/frontend/src/data.jsx"
      provides: "Data layer — STEPS, computeClassification, all constants"
      exports: ["I", "STEPS", "PHASES", "DRF", "WORK_TYPES", "EC_ELEMENTS", "computeClassification", "refineDuty", "ecFactors"]
    - path: "v2/frontend/src/components.jsx"
      provides: "Shared input controls"
      exports: ["Icon", "Check", "StepInput", "initialAnswer", "answerValid"]
    - path: "v2/frontend/src/styles.css"
      provides: "Brand CSS"
      contains: "--ui: \"Hanken Grotesk\""
    - path: "v2/frontend/index.html"
      provides: "HTML shell with brand font link"
      contains: "fonts.googleapis.com"
  key_links:
    - from: "v2/frontend/src/components.jsx"
      to: "v2/frontend/src/data.jsx"
      via: "ES module import"
      pattern: "import.*from.*./data.jsx"
    - from: "v2/frontend/src/main.jsx"
      to: "v2/frontend/src/styles.css"
      via: "ES module import"
      pattern: "import.*./styles.css"
---

<objective>
Port the prototype's data layer (`data.jsx`), shared input controls (`components.jsx`), brand CSS (`styles.css`), and HTML/entry-point wiring (`index.html`, `main.jsx`) into the Vite project as ES modules. No IIFE globals, no `React.createElement` — ES module exports and JSX throughout.

Purpose: Establishes the dependency-free foundation that `conversation.jsx`, `document.jsx`, and `app.jsx` (Plan 03) will import from. Delivers FE-01 (file structure) and FE-03 (brand typography).
Output: 5 files created/updated; `npm run build` clean; brand fonts and CSS custom properties in place.
</objective>

<execution_context>
@/home/charles/.claude/get-shit-done/workflows/execute-plan.md
@/home/charles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/charles/job_description_builder/.planning/PROJECT.md
@/home/charles/job_description_builder/.planning/ROADMAP.md
@/home/charles/job_description_builder/.planning/phases/13-frontend-spa-shell/13-RESEARCH.md
@/home/charles/job_description_builder/.planning/phases/13-frontend-spa-shell/13-01-SUMMARY.md

<interfaces>
<!-- Prototype source files to port — read these in full before writing -->
Source: Job Description Builder/jd-builder/data.jsx
Source: Job Description Builder/jd-builder/components.jsx
Source: Job Description Builder/jd-builder/styles.css

<!-- Conversion rule (RESEARCH.md Pattern 1) -->
IIFE conversion:
  BEFORE: (function() { ... window.JD_DATA = { ... }; })();
  AFTER:  Remove IIFE wrapper. Replace `window.JD_X = { a, b, c }` with `export { a, b, c }`.
  Never leave dangling window.JD_* references.

<!-- React.createElement → JSX (RESEARCH.md Pattern 2) -->
  BEFORE: React.createElement('div', { className: 'ask' }, ...)
  AFTER:  <div className="ask">...</div>
  Vite's @vitejs/plugin-react handles JSX compilation.

<!-- dangerouslySetInnerHTML safety note (RESEARCH.md Pitfall 5 + security requirement) -->
In components.jsx, the Icon component uses:
  dangerouslySetInnerHTML={{ __html: path }}
This is intentional — paths are hardcoded string literals from data.jsx, not user input.
Add comment: // Icon paths are string literals from data.jsx — not user input; XSS-safe

<!-- Google Fonts link (RESEARCH.md Pattern 4) -->
Use variable font URL format for Hanken Grotesk:
  family=Hanken+Grotesk:ital,wght@0,300..800;1,300..800
NOT discrete weights like wght@400;500;550 — variable range is required for weights 550, 680, 720, 750.
Full link tag:
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Hanken+Grotesk:ital,wght@0,300..800;1,300..800&family=Spectral:ital,wght@0,400;0,600;0,700;1,400&family=Spline+Sans+Mono:wght@400;500;600&display=swap" rel="stylesheet">

<!-- main.jsx current content (read before editing) -->
v2/frontend/src/main.jsx currently imports './App.jsx' and './index.css'.
Must become:
  import App from './app.jsx'   // lowercase — matches new file
  import './styles.css'          // replaces index.css

<!-- CSS :root block from prototype styles.css (exact values to verify against) -->
--ui:   "Hanken Grotesk", system-ui, sans-serif;
--doc:  "Spectral", Georgia, serif;
--mono: "Spline Sans Mono", ui-monospace, monospace;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port data.jsx and components.jsx as ES modules</name>
  <files>v2/frontend/src/data.jsx, v2/frontend/src/components.jsx</files>
  <read_first>
    - Job Description Builder/jd-builder/data.jsx — read in full before writing
    - Job Description Builder/jd-builder/components.jsx — read in full before writing
  </read_first>
  <action>
Create v2/frontend/src/data.jsx by converting the prototype data.jsx:
1. Remove the IIFE wrapper: delete `(function () {` at line 7 and `})();` at the end.
2. Remove `window.JD_DATA = { ... };` — replace with `export { I, STEPS, PHASES, DRF, WORK_TYPES, DUTY_SUGGESTIONS, QUAL_DEFAULT, EC_ELEMENTS, computeClassification, refineDuty, ecFactors };` at the bottom of the file.
3. Do NOT add any import statements — data.jsx has no dependencies.
4. Do NOT convert React.createElement calls in data.jsx — data.jsx has none (it is pure data + logic).
5. Preserve every constant, every function, every comment verbatim. This is a module system conversion, not a rewrite.

Create v2/frontend/src/components.jsx by converting the prototype components.jsx:
1. Add at the top: `import { I, STEPS } from './data.jsx';` — replace the `const D = window.JD_DATA;` reference with named imports. Review the prototype carefully to identify exactly which exports from data.jsx are used.
2. Add `import React, { useState, useRef, useEffect } from 'react';` if hooks are used directly (check prototype).
3. Remove the IIFE wrapper.
4. Replace `window.JD_COMP = { Icon, Check, StepInput, initialAnswer, answerValid };` with `export { Icon, Check, StepInput, initialAnswer, answerValid };`.
5. Convert all `React.createElement(...)` calls to JSX throughout.
6. In the Icon component, add the comment `// Icon paths are string literals from data.jsx — not user input; XSS-safe` directly above the `dangerouslySetInnerHTML` usage.
7. Preserve all component logic, all className values, all event handlers verbatim.
  </action>
  <verify>
    <automated>cd /home/charles/job_description_builder/v2/frontend && node --input-type=module --eval "import { STEPS, computeClassification } from './src/data.jsx'" 2>&1 | head -5; grep -c "export" src/data.jsx; grep -q "dangerouslySetInnerHTML" src/components.jsx && grep -q "XSS-safe" src/components.jsx && echo "Icon comment OK"</automated>
  </verify>
  <done>
    - v2/frontend/src/data.jsx exists; contains `export {` at end of file; no `window.JD_DATA` references
    - v2/frontend/src/components.jsx exists; contains `export { Icon, Check, StepInput, initialAnswer, answerValid }`; contains "XSS-safe" comment near dangerouslySetInnerHTML; no `window.JD_COMP` references
    - `npm run build` from v2/frontend/ exits 0 (build may fail on missing app.jsx — that is acceptable; the specific error must be "cannot find module ./app.jsx" not a syntax error in data.jsx or components.jsx)
  </done>
</task>

<task type="auto">
  <name>Task 2: Port styles.css, update index.html with Google Fonts, update main.jsx</name>
  <files>v2/frontend/src/styles.css, v2/frontend/index.html, v2/frontend/src/main.jsx</files>
  <read_first>
    - Job Description Builder/jd-builder/styles.css — read in full before writing (this is the source of truth)
    - v2/frontend/index.html — read current content before editing
    - v2/frontend/src/main.jsx — read current content before editing
    - v2/frontend/src/index.css — note this file will become dead weight; do NOT delete it (leave for Plan 03 cleanup or leave empty)
  </read_first>
  <action>
**styles.css:** Create v2/frontend/src/styles.css as a copy of the prototype styles.css verbatim. Do not modify any rule. The :root block must contain exactly:
```css
--ui:   "Hanken Grotesk", system-ui, sans-serif;
--doc:  "Spectral", Georgia, serif;
--mono: "Spline Sans Mono", ui-monospace, monospace;
```

**index.html:** Read the current v2/frontend/index.html. Add three `<link>` tags inside `<head>`, before the `<title>` or closing `</head>`, for the Google Fonts CDN. Use variable font range format for Hanken Grotesk:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Hanken+Grotesk:ital,wght@0,300..800;1,300..800&family=Spectral:ital,wght@0,400;0,600;0,700;1,400&family=Spline+Sans+Mono:wght@400;500;600&display=swap" rel="stylesheet">
```
Do NOT use discrete weight values like `wght@400;550;700` for Hanken Grotesk — variable range `300..800` is required for non-standard weights (550, 680, 720, 750) used in the CSS.

**main.jsx:** Replace the current content entirely with:
```jsx
import React from 'react'
import { createRoot } from 'react-dom/client'
import App from './app.jsx'
import './styles.css'

const root = createRoot(document.getElementById('root'))
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```
This fixes the case-sensitivity issue (RESEARCH.md Pitfall 1: `./App.jsx` → `./app.jsx` for Linux) and replaces the index.css import with styles.css.
  </action>
  <verify>
    <automated>grep -q "Hanken Grotesk" /home/charles/job_description_builder/v2/frontend/src/styles.css && grep -q "Spectral" /home/charles/job_description_builder/v2/frontend/src/styles.css && grep -q "Spline Sans Mono" /home/charles/job_description_builder/v2/frontend/src/styles.css && echo "CSS fonts OK"; grep -q "fonts.googleapis.com" /home/charles/job_description_builder/v2/frontend/index.html && echo "HTML font link OK"; grep -q "./app.jsx" /home/charles/job_description_builder/v2/frontend/src/main.jsx && grep -q "./styles.css" /home/charles/job_description_builder/v2/frontend/src/main.jsx && echo "main.jsx OK"; grep -q "300..800" /home/charles/job_description_builder/v2/frontend/index.html && echo "variable font range OK"</automated>
  </verify>
  <done>
    - v2/frontend/src/styles.css exists; contains `--ui`, `--doc`, `--mono` custom properties with correct font names; file size > 1000 bytes (prototype CSS is ~500 lines)
    - v2/frontend/index.html contains `fonts.googleapis.com` link with `300..800` variable range for Hanken Grotesk
    - v2/frontend/src/main.jsx imports `./app.jsx` (lowercase) and `./styles.css`; no reference to `./App.jsx` or `./index.css`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Google Fonts CDN | fonts.googleapis.com loaded in HTML head; no data sent, fonts received |
| data.jsx → components.jsx | Icon paths injected via dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | dangerouslySetInnerHTML in Icon component | accept | All path strings are compile-time literals in data.jsx — not user input, not fetched from API. Comment added in code: "XSS-safe". No runtime path construction. |
| T-13-02-02 | Information Disclosure | Google Fonts CDN request | accept | Browser sends Referer header to fonts.googleapis.com; this is standard CDN font-loading behaviour for a local dev tool. No PII transmitted. |
</threat_model>

<verification>
1. `ls v2/frontend/src/{data,components}.jsx v2/frontend/src/styles.css` — all three exist
2. `grep -c "export" v2/frontend/src/data.jsx` — at least 1 export line
3. `grep -q "window.JD_DATA" v2/frontend/src/data.jsx && echo FAIL || echo OK` — must print OK
4. `grep -q "window.JD_COMP" v2/frontend/src/components.jsx && echo FAIL || echo OK` — must print OK
5. `grep -q "Hanken Grotesk" v2/frontend/src/styles.css && echo OK`
6. `grep -q "300..800" v2/frontend/index.html && echo "variable font range OK"`
7. `grep -q "./app.jsx" v2/frontend/src/main.jsx && echo OK`
</verification>

<success_criteria>
- data.jsx: ES module with named exports; no IIFE; no window.JD_DATA; all prototype constants and functions preserved verbatim
- components.jsx: ES module; imports from ./data.jsx; JSX throughout; XSS comment on Icon; exports {Icon, Check, StepInput, initialAnswer, answerValid}
- styles.css: verbatim copy of prototype styles.css; contains --ui, --doc, --mono font vars
- index.html: Google Fonts <link> with variable range `300..800` for Hanken Grotesk
- main.jsx: imports ./app.jsx (lowercase) and ./styles.css
</success_criteria>

<output>
After completion, create `/home/charles/job_description_builder/.planning/phases/13-frontend-spa-shell/13-02-SUMMARY.md`
</output>
