---
phase: 05-og-classification
plan: 02
subsystem: ai
tags: [instructor, pydantic, ollama, dashscope, openai-compatible, singleton]

# Dependency graph
requires:
  - phase: 05-og-classification
    plan: 01
    provides: og_definitions table, og_db fixture
provides:
  - OGCandidate, OGRankingResult, PolicyAdjacencyResult Pydantic models
  - OG_LEVELS static lookup (10 OG codes, 56 levels total)
  - og_instructor_client module-level singleton
  - SYSTEM_PROMPT and POLICY_DETECTION_PROMPT constants
  - build_og_context() prompt assembler
affects: [05-03-og-classifier, 05-04-templates]

# Tech tracking
tech-stack:
  added: []
  patterns: [module-level instructor singleton (httpx pool reuse), Pydantic Field constraints for rank and confidence, Mode.JSON for Ollama, cloud fallback via DashScope]

key-files:
  created:
    - app/ai/og_ranking.py

key-decisions:
  - "OG_LEVELS hardcoded as static dict — never generated by LLM (architecture non-negotiable: prevents drift between LLM output and CA-defined level ranges)"
  - "og_instructor_client built at module import time — never per-request (architectural pitfall: would create httpx connection pool on every call)"
  - "OGCandidate.evidence_quotes defaults to [] so the LLM can omit quotes when not applicable"
  - "build_og_context() truncates work description at 500 chars and OG fields at 400/300 chars to fit Ollama num_ctx=16384 budget"
  - "Ranks in [1, 3] (not [1, 10] like NOC) — top-3 is the explicit CLASS-01 contract"

patterns-established:
  - "OG ranking AI contract: Pydantic model + module-level singleton + prompt constants all colocated in app/ai/og_ranking.py"
  - "Static lookup table pattern for level ranges (no LLM generation of structured metadata)"

requirements-completed: [CLASS-01, CLASS-02, CLASS-03]

# Metrics
duration: 2min
completed: 2026-06-02
---

# Phase 5 Plan 02 Summary

**app/ai/og_ranking.py: Pydantic AI output models + OG_LEVELS static lookup + module-level instructor singleton + prompt constants**

## Performance

- **Duration:** 2 min
- **Started:** 2026-06-02
- **Completed:** 2026-06-02
- **Tasks:** 1/1
- **Files modified:** 1 (created)

## Accomplishments

- `OGCandidate` Pydantic model with rank ∈ [1,3], confidence ∈ [0,1], evidence_quotes list — rejects out-of-range input
- `OGRankingResult` with min/max length 1-3 — enforces top-3 contract
- `PolicyAdjacencyResult` for Step 2 binary classification (is_policy_adjacent + confidence + verbatim phrases)
- `OG_LEVELS` static dict: AS [1-8], CR [1-6], PM [1-6], PE [1-7], EC [1-7], IT [1-4], CS [1-5], EX [1-5], IS [1-7], GT [1-8]
- `og_instructor_client` module-level singleton (`AsyncInstructor`) — reuses httpx connection pool
- `SYSTEM_PROMPT` and `POLICY_DETECTION_PROMPT` constants — fully self-contained
- `build_og_context()` assembles NOC + OG definitions into a single prompt string with size budgets

## Task Commits

1. **Task 1: og_ranking.py** — `6180ce9` (feat)

## Files Created/Modified

- `app/ai/og_ranking.py` — NEW: 152 lines; full AI contract for OG ranking

## Decisions Made

- `OGCandidate.evidence_quotes` defaults to `[]` (factory) so the LLM can omit when not applicable
- `OGRankingResult.candidates` min_length=1 (not 3) — allows LLM to return fewer if no third OG is plausible
- `build_og_context()` truncates work description at 500 chars to keep total prompt under Ollama num_ctx=16384
- `og_instructor_client` uses `Mode.JSON` for both Ollama and DashScope — both providers support JSON mode

## Deviations from Plan

None - plan executed exactly as written. The plan's reference to `from typing import NamedTuple` was removed as unused; the rest of the structure matches.

## Issues Encountered

None.

## Next Phase Readiness

- Plan 05-03 (`og_classifier.py` service pipeline) can proceed — all AI contract types and the singleton are in place
- All 9 of 11 test_og_ranking stubs now pass; the 2 remaining skips (verbatim_guardrail, asec_alert) depend on app.services.og_classifier (Plan 05-03)

---
*Phase: 05-og-classification*
*Completed: 2026-06-02*
