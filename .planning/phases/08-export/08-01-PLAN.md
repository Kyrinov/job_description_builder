---
phase: 08-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_export.py
  - templates/docx/work_description_template.docx
  - scripts/build_docx_template.py
autonomous: true
requirements: [EXP-01]
must_haves:
  truths:
    - "A pytest fixture exists that builds a WorkDescription in stage='jes_scored' with full ProvenanceTags"
    - "A docxtpl-compatible .docx template file exists with the TBS WD section structure and Jinja2 variables"
    - "test_export.py defines the contract tests for the pre-export validator, manifest builder, and DOCX export (skipping until service lands)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "export_db fixture + exported_wd helper"
      contains: "def export_db"
    - path: "tests/test_export.py"
      provides: "Phase 8 contract tests"
      contains: "def test_"
    - path: "templates/docx/work_description_template.docx"
      provides: "docxtpl template with TBS WD sections + Jinja2 vars"
      min_lines: 0
    - path: "scripts/build_docx_template.py"
      provides: "Reproducible generator for the .docx template"
      contains: "from docxtpl"
  key_links:
    - from: "tests/test_export.py"
      to: "tests/conftest.py"
      via: "export_db fixture injection"
      pattern: "export_db"
---

<objective>
Establish the Phase 8 test scaffold and the docxtpl template artifact before any
service code is written. This is the interface-first foundation: it defines the
fixture that seeds an exportable WorkDescription, the contract tests that the
export service must satisfy, and the .docx template file (with TBS WD section
structure and Jinja2 variables) that the service renders into.

Purpose: Downstream plans (08-02 service, 08-03 router, 08-04 templates) build
against these contracts without re-deriving them. The .docx template is a binary
artifact with no codebase analog, so it is built here via a reproducible script.

Output:
- `tests/conftest.py` gains an `export_db` fixture and `make_exported_wd()` helper
- `tests/test_export.py` with skipping contract tests for validator + manifest + DOCX
- `scripts/build_docx_template.py` that generates the template deterministically
- `templates/docx/work_description_template.docx` committed as the rendered output
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-export/08-CONTEXT.md
@.planning/phases/08-export/08-PATTERNS.md

<interfaces>
<!-- Data model the export renders from. Extracted from app/models/work_description.py. -->
<!-- Executor uses these field names verbatim — do NOT invent or rename fields. -->

ProvenanceTag fields:
  source_type: Literal["NOC","CA","JES","TBS_OG_DEF","TBS_DIRECTIVE","QUAL_STD","DRF","ADVISOR","AI_GENERATED"]
  source_id: str
  source_version: str
  source_url: Optional[str]
  retrieved_date: date
  model_name: Optional[str]
  prompt_version: Optional[str]
  modified_by_advisor: bool

JESFactorScore fields:
  factor_name: str
  level: int                 # -1 == failed-factor sentinel
  points: Optional[int]      # None == incomplete (blocking for export per D-02)
  rationale: str
  provenance: ProvenanceTag
  advisor_adjusted: bool

DraftDuty fields:
  id: UUID
  text: str
  provenance: ProvenanceTag
  advisor_modified: bool
  advisor_modified_text: Optional[str]

WorkDescription (export-relevant fields):
  id: UUID; session_id: str
  position_title, position_number, og_level, supervisor_title,
  supervisor_position_number: Optional[str]
  review_date: Optional[date]
  organizational_context: Optional[DraftText]   # has .text and .provenance
  raw_input: str
  confirmed_noc: Optional[NOCMatch]             # has .provenance
  og_recommendation: Optional[OGRecommendation] # has .provenance and .cited_articles: list[ProvenanceTag]
  confirmed_og: Optional[str]; confirmed_level: Optional[str]
  draft_duties: list[DraftDuty]
  advisor_additions: list[DraftDuty]            # source_type="ADVISOR"
  jes_scores: list[JESFactorScore]
  jes_total_points: Optional[int]
  stage: Literal["input","noc_mapped","og_classified","jd_drafted","jes_scored","exported"]
  export_hash: Optional[str]; exported_at: Optional[datetime]

wd_store API (app/services/wd_store.py):
  save_work_description(conn, wd) -> None
  load_work_description(conn, wd_id) -> WorkDescription | None
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add export_db fixture and make_exported_wd helper to conftest.py</name>
  <read_first>
    - tests/conftest.py (full file — append a new fixture following jes_db pattern at lines 267-327)
    - app/models/work_description.py (field names for WorkDescription, JESFactorScore, DraftDuty, ProvenanceTag, NOCMatch, OGRecommendation, DraftText)
    - app/services/wd_store.py (save_work_description / load_work_description signatures)
  </read_first>
  <action>
    Append to tests/conftest.py a new fixture `export_db(tmp_path)` modeled exactly on
    the existing `jes_db` fixture (lines 267-327). It must:
    1. Create a temp SQLite DB at `str(tmp_path / "test_export.db")` via
       `from app.db import create_schema, get_connection`, call `create_schema(con)`,
       `con.commit()`, `yield db_path`, then `con.close()`.

    Also add a module-level helper function (NOT a fixture):
    `make_exported_wd(db_path: str, *, complete: bool = True) -> str` that:
    - Imports inside the function (defer imports, mirror _make_jd_drafted_wd in test_jes_scoring.py):
      `from app.db import get_connection`,
      `from app.models.work_description import (DraftDuty, DraftText, JESFactorScore, NOCMatch, OGRecommendation, ProvenanceTag, WorkDescription)`,
      `from app.services.wd_store import save_work_description`,
      `from datetime import date`.
    - Builds a WorkDescription with stage="jes_scored" and ALL of these populated:
      position_title="Senior Policy Analyst", position_number="12345",
      og_level="EC-05", supervisor_title="Manager, Policy",
      supervisor_position_number="00001", review_date=date(2026, 6, 2),
      confirmed_og="EC", confirmed_level="EC-05", session_id="test-session",
      raw_input="Develops policy options for senior management.".
    - organizational_context = DraftText(text="Operates within the Policy Branch.",
      provenance=ProvenanceTag(source_type="TBS_OG_DEF", source_id="EC",
      source_version="TBS-OCHRO-OG.txt", retrieved_date=date.today())).
    - confirmed_noc = NOCMatch(noc_code="41401", noc_title="Economists and economic policy researchers and analysts",
      teer_level="1", confidence=0.9, rationale="Strong match",
      matched_duty_statements=["Conduct economic analysis."],
      provenance=ProvenanceTag(source_type="NOC", source_id="41401",
      source_version="NOC 2021 v1.0", retrieved_date=date.today())).
    - og_recommendation = OGRecommendation(og_code="EC", og_name="Economics and Social Science Services",
      level="EC-05", confidence=0.85, rationale="Policy work directed to Canadians",
      provenance=ProvenanceTag(source_type="TBS_OG_DEF", source_id="EC",
      source_version="TBS-OCHRO-OG.txt", retrieved_date=date.today()),
      cited_articles=[ProvenanceTag(source_type="TBS_DIRECTIVE", source_id="Directive 4.2.1",
      source_version="TBS Directive on Classification 2021", retrieved_date=date.today())],
      confirmed_by_advisor=True).
    - draft_duties = [one authoritative duty: DraftDuty(text="Conduct economic analysis of policy options.",
      provenance=ProvenanceTag(source_type="NOC", source_id="41401",
      source_version="NOC 2021 v1.0", retrieved_date=date.today()))].
    - advisor_additions = [one advisor duty: DraftDuty(text="Liaise with provincial counterparts.",
      advisor_modified=True,
      provenance=ProvenanceTag(source_type="ADVISOR", source_id="advisor",
      source_version="manual entry", retrieved_date=date.today(), modified_by_advisor=True))].
    - jes_scores: if complete is True, two valid factors:
      JESFactorScore(factor_name="Decision making", level=3, points=35, rationale="High latitude",
        provenance=ProvenanceTag(source_type="JES", source_id="EC/Decision making",
        source_version="JES v1.0", retrieved_date=date.today())),
      JESFactorScore(factor_name="Communication", level=2, points=30, rationale="Explains findings",
        provenance=ProvenanceTag(source_type="JES", source_id="EC/Communication",
        source_version="JES v1.0", retrieved_date=date.today())).
      jes_total_points=65.
      If complete is False, make the second factor INCOMPLETE:
      JESFactorScore(factor_name="Communication", level=-1, points=None,
      rationale="Scoring failed after 3 retries", provenance=<same JES tag>),
      and set jes_total_points=35.
    - Open connection via get_connection(db_path), call save_work_description(conn, wd),
      conn.close(), and return str(wd.id).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/conftest.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py contains `def export_db(`
    - tests/conftest.py contains `def make_exported_wd(`
    - tests/conftest.py contains `stage="jes_scored"` within make_exported_wd
    - tests/conftest.py contains `source_type="ADVISOR"`
    - tests/conftest.py contains `complete: bool = True`
    - `python -c "import ast; ast.parse(open('tests/conftest.py').read())"` exits 0
  </acceptance_criteria>
  <done>export_db fixture and make_exported_wd helper present; conftest.py parses cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 8 contract tests in test_export.py</name>
  <read_first>
    - tests/test_jes_scoring.py lines 1-60 (bootstrap fixture + _make_*_wd defer-import + skip-on-ImportError pattern)
    - tests/conftest.py (the export_db fixture and make_exported_wd helper added in Task 1)
    - .planning/phases/08-export/08-CONTEXT.md (D-01, D-02, D-05, D-06, D-07, D-08 — the behaviors to assert)
  </read_first>
  <action>
    Create tests/test_export.py. Follow the test_jes_scoring.py structure: module docstring,
    `pytest`, defer all `from app.services.export_service import ...` imports inside each test
    body wrapped in try/except ImportError → `pytest.skip("export_service not yet implemented")`.
    Use the `export_db` fixture and `make_exported_wd` helper from conftest.

    Write these tests (all skip until 08-02 lands the service):

    1. test_generate_export_returns_file_bytes(export_db):
       wd_id = make_exported_wd(export_db, complete=True);
       result = await generate_export(wd_id=wd_id, db_path=export_db);
       assert result["file_bytes"] is non-empty bytes (len > 0);
       assert result["filename"].endswith(".docx");
       assert "export_hash" in result and len(result["export_hash"]) == 64 (SHA-256 hex).
       Mark `@pytest.mark.asyncio`.

    2. test_export_advances_stage_to_exported(export_db):
       After generate_export on a complete WD, load_work_description shows
       stage == "exported", export_hash is not None, exported_at is not None. asyncio.

    3. test_export_blocked_on_incomplete_jes(export_db):
       wd_id = make_exported_wd(export_db, complete=False);
       with pytest.raises(ValueError) as exc: await generate_export(...);
       assert "Communication" in str(exc.value) (the failed factor is named per D-01);
       assert "JES" in str(exc.value) or "scoring" in str(exc.value). asyncio.

    4. test_export_blocked_does_not_advance_stage(export_db):
       After a blocked export attempt on complete=False WD, load_work_description shows
       stage still == "jes_scored" (NOT "exported"). asyncio.

    5. test_validate_export_returns_failed_factor_names(export_db):
       Import `validate_export_readiness` from app.services.export_service;
       wd = load_work_description for the complete=False WD;
       errors = validate_export_readiness(wd);
       assert errors is a non-empty list and any "Communication" in e for e in errors;
       for the complete=True WD, validate_export_readiness(wd) returns []. (sync, not asyncio)

    6. test_version_manifest_includes_all_sources(export_db):
       Import `build_version_manifest` from app.services.export_service;
       wd = load complete WD; manifest = build_version_manifest(wd);
       assert manifest is a list of dicts each with keys
       {"source_type","source_id","source_version","retrieved_date"};
       assert at least one entry has source_type == "NOC", one == "JES",
       one == "TBS_OG_DEF", one == "ADVISOR";
       assert no duplicate (source_type, source_id, source_version) tuples. (sync)

    7. test_pdf_route_returns_501 — DEFER to 08-03; do NOT write here (router not in this plan).
       Add a comment noting it is covered in test for the router plan.

    Use `from datetime import date` only where needed. Mark async tests with
    `@pytest.mark.asyncio` (pytest-asyncio is already used by test_jes_scoring.py).
  </action>
  <verify>
    <automated>python -m pytest tests/test_export.py -q 2>&1 | grep -E "skipped|passed|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_export.py contains `def test_export_blocked_on_incomplete_jes`
    - tests/test_export.py contains `def test_version_manifest_includes_all_sources`
    - tests/test_export.py contains `validate_export_readiness`
    - tests/test_export.py contains `build_version_manifest`
    - tests/test_export.py contains `pytest.skip`
    - `python -m pytest tests/test_export.py -q` collects without collection errors (tests skip, not error)
  </acceptance_criteria>
  <done>test_export.py defines all 6 contract tests; they skip cleanly because export_service does not yet exist.</done>
</task>

<task type="auto">
  <name>Task 3: Build the docxtpl TBS WD template via a reproducible script</name>
  <read_first>
    - .planning/phases/08-export/08-CONTEXT.md (D-04 section structure, D-05 Jinja2-only, D-06 advisor marker, D-07 manifest table)
    - .planning/phases/08-export/08-PATTERNS.md ("No Analog Found" section — required Jinja2 variable names)
    - data/directive_on_classification.txt (read first ~200 lines to confirm TBS WD section names — position identification, organizational context, key activities, classification)
  </read_first>
  <action>
    docxtpl==0.18.0 and python-docx==1.1.2 are already in requirements.txt — do NOT add deps.

    Create scripts/build_docx_template.py that uses python-docx to construct a .docx with
    docxtpl Jinja2 placeholders, then saves it to templates/docx/work_description_template.docx.
    The script must `os.makedirs("templates/docx", exist_ok=True)` first.

    The template follows the TBS Work Description format (D-04). Build these sections as
    docx headings + paragraphs/tables, embedding these EXACT docxtpl Jinja2 variables
    (these names are the contract the service in 08-02 fills via context dict):

    1. Title heading: "Work Description"
    2. Section "1. Position Identification" — a table with rows:
       Position Title: {{ position_title }}
       Position Number: {{ position_number }}
       Group and Level: {{ og_level }}
       Supervisor Title: {{ supervisor_title }}
       Supervisor Position Number: {{ supervisor_position_number }}
       Review Date: {{ review_date }}
    3. Section "2. Organizational Context":
       paragraph {{ organizational_context_text }}
       citation paragraph (italic): "Source: {{ organizational_context_source }}"
    4. Section "3. Key Activities (Duties)":
       a paragraph loop over duties:
       {% for duty in duties %}
         {{ duty.text }}
         (italic citation line) Source: {{ duty.source_id }} ({{ duty.source_version }})
         {% if duty.is_advisor %}  [advisor-added / not from authoritative source] {% endif %}
       {% endfor %}
       Advisor-added duties must render with the literal label
       "advisor-added / not from authoritative source" and italic styling (D-06).
    5. Section "4. Classification — JES Scoring":
       a table with header row Factor | Level | Points | Source, then loop:
       {% for f in jes_scores %} {{ f.factor_name }} | {{ f.level }} | {{ f.points }} |
       {{ f.source_id }} ({{ f.source_version }}) {% endfor %}
       and a Total Points: {{ jes_total_points }} line.
    6. Section "5. Source Document Version Manifest":
       a table with header Source | ID | Version | Date, then loop:
       {% for m in manifest %} {{ m.source_type }} | {{ m.source_id }} |
       {{ m.source_version }} | {{ m.retrieved_date }} {% endfor %}

    For docxtpl loops/conditionals inside tables, use the docxtpl table-row syntax
    (`{%tr for ... %}` / `{%tr endfor %}` placed in cells) — consult docxtpl docs if unsure
    via `npx --yes ctx7@latest library docxtpl "table row loop syntax"`.

    Run the script as the final step so the .docx is generated:
    `python scripts/build_docx_template.py`.

    Save final path: templates/docx/work_description_template.docx.
  </action>
  <verify>
    <automated>python scripts/build_docx_template.py && python -c "from docxtpl import DocxTemplate; t=DocxTemplate('templates/docx/work_description_template.docx'); print(sorted(t.get_undeclared_template_variables()))"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/build_docx_template.py contains `from docxtpl` or `import docx`
    - templates/docx/work_description_template.docx exists (file size > 0)
    - `DocxTemplate(...).get_undeclared_template_variables()` includes `position_title`
    - `get_undeclared_template_variables()` includes `duties`
    - `get_undeclared_template_variables()` includes `jes_scores`
    - `get_undeclared_template_variables()` includes `manifest`
    - The script run exits 0
  </acceptance_criteria>
  <done>Template .docx generated with all required Jinja2 variables; docxtpl can load and parse it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → SQLite temp DB | Synthetic data only; no external input |
| script → filesystem | Writes a single .docx into templates/docx/ (committed artifact) |

## ASVS L1 Threat Register (STRIDE)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | docxtpl template file | accept | Template is a committed, version-controlled artifact built deterministically by scripts/build_docx_template.py; no runtime mutation. SHA tracked via git. |
| T-08-02 | Information Disclosure | test fixtures | accept | Fixtures use synthetic data only (no PII, no real WD records). |
| T-08-03 | Injection | docxtpl Jinja2 vars | mitigate | Template uses variable substitution only (D-05); no eval of user-controlled template strings. Service (08-02) passes structured context, not raw template text. |
</threat_model>

<verification>
- `python -m pytest tests/test_export.py -q` collects and skips cleanly (no collection errors)
- `python -c "import ast; ast.parse(open('tests/conftest.py').read())"` exits 0
- `templates/docx/work_description_template.docx` loads in docxtpl with all required vars
- Existing suite still green: `python -m pytest -q` shows no new failures introduced
</verification>

<success_criteria>
- export_db fixture + make_exported_wd helper (with complete/incomplete modes) in conftest.py
- 6 skipping contract tests in test_export.py covering validator, manifest, DOCX bytes, stage advancement, and the D-01 blocking gate
- Reproducible scripts/build_docx_template.py + generated templates/docx/work_description_template.docx with all docxtpl variables (position_title, duties, jes_scores, manifest)
</success_criteria>

<output>
After completion, create `.planning/phases/08-export/08-01-SUMMARY.md`
</output>
