---
phase: 08-export
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - templates/wizard/step_export.html
  - templates/partials/export_result.html
  - templates/partials/jes_scores.html
  - app/static/css/main.css
autonomous: false
requirements: [EXP-01]
must_haves:
  truths:
    - "The export wizard step shows position summary, JES summary, version manifest preview, inline validation errors, and a Download DOCX CTA"
    - "After successful HTMX export, the export_result partial confirms success and shows the SHA-256 hash"
    - "The JES scoring partial's 'Continue to Export' CTA is activated (no longer aria-disabled) and links to /wizard/export"
    - "A human verifies the full export flow end-to-end in the browser"
  artifacts:
    - path: "templates/wizard/step_export.html"
      provides: "terminal wizard step for export"
      contains: "/export/"
    - path: "templates/partials/export_result.html"
      provides: "HTMX success partial"
      contains: "export-result"
    - path: "app/static/css/main.css"
      provides: "Layer 11 export component styles"
      contains: "Layer 11"
  key_links:
    - from: "templates/partials/jes_scores.html"
      to: "/wizard/export"
      via: "activated Continue to Export CTA"
      pattern: "/wizard/export"
    - from: "templates/wizard/step_export.html"
      to: "/export/{wd_id}/docx"
      via: "Download DOCX CTA"
      pattern: "/export/.*docx"
---

<objective>
Ship the export UI: the terminal wizard step (D-09), the HTMX success partial,
the activation of the JES→Export CTA, and the CSS for export components. Then a
human verifies the whole flow in the browser.

Purpose: Completes EXP-01 success criterion 1 (downloadable file via UI) and 4
(advisor-added marker visible) from the advisor's perspective, and closes the
Phase 7→8 wizard handoff that left "Continue to Export" disabled.

Output: step_export.html, export_result.html, activated jes_scores.html CTA,
CSS layer 11, and a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-export/08-CONTEXT.md
@.planning/phases/08-export/08-PATTERNS.md
@.planning/phases/08-export/08-03-SUMMARY.md

<interfaces>
<!-- Existing template patterns to follow. -->

step_jes.html structure: extends base.html; {% block title %}; {% block content %}
with <section id="wizard-step">; an hx-post form with hx-target/hx-swap/hx-indicator/
hx-disabled-elt; a button.button--primary.

jes_scores.html (the partial 08-04 must EDIT) currently ends with, at lines 27-28:
```html
<a href="/wizard/export?wd_id={{ wd_id }}" class="button button--secondary"
   aria-disabled="true" title="Available in Phase 8">Continue to Export</a>
```

main.css existing layers (header comment lines 4-15): Layer 8 = Phase 5 OG,
Layer 10 = JES Scoring (Phase 7). Tail of file ends with .jes-* component classes
(.jes-total-points last). New CSS goes at the END as "Layer 11: Export (Phase 8)".

Export endpoints (from 08-03): GET /export/{wd_id}/docx (file or HTMX partial),
GET /export/{wd_id}/pdf (501). Wizard route: /wizard/export?wd_id=...

generate_export returns export_hash + filename; the wizard route passes only wd_id
to the template, so validation errors / summary must be fetched/rendered another way:
the step_export.html page links to /export/{wd_id}/docx and lets the server return a
422 with the named factors if blocked. (Keep the page simple — no extra service call
from main.py is in scope; the Download CTA surfaces the block via the HTMX response.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create step_export.html and export_result.html, activate JES CTA</name>
  <read_first>
    - templates/wizard/step_jes.html (full file — base extension, block structure, HTMX form, button classes)
    - templates/partials/jes_scores.html (full file — the "Continue to Export" CTA at lines 27-28 to activate; the .jes-source citation pattern to mirror)
    - .planning/phases/08-export/08-PATTERNS.md (step_export.html + export_result.html full file patterns)
    - .planning/phases/08-export/08-CONTEXT.md (D-09 page contents; D-06 advisor marker; D-10 re-export allowed)
  </read_first>
  <action>
    (a) Create templates/wizard/step_export.html:
    - `{% extends "base.html" %}`, `{% block title %}Export Work Description — JD Builder{% endblock %}`,
      `{% block content %}`...`{% endblock %}`.
    - `<section id="wizard-step">` with `<h1>Export Work Description</h1>` and a lead
      paragraph "Download the completed Work Description as a DOCX file."
    - Hidden input: `<input type="hidden" name="wd_id" value="{{ wd_id }}">`.
    - A "Version Manifest Preview" note: since the wizard route only passes wd_id, render a
      static explanatory paragraph: "The exported document includes a version manifest of every
      source document (NOC, CA, JES, TBS OG definitions, directive) used to build this Work
      Description." (The actual manifest is rendered into the DOCX by the service.)
    - Download CTA using HTMX so a blocked export shows the named factors inline:
      ```html
      <a href="/export/{{ wd_id }}/docx"
         hx-get="/export/{{ wd_id }}/docx"
         hx-target="#export-result"
         hx-swap="innerHTML"
         hx-indicator="#export-spinner"
         class="button button--primary" id="download-btn">
          Download DOCX
      </a>
      <span id="export-spinner" class="htmx-indicator" aria-label="Generating DOCX">Generating…</span>
      <div id="export-result" aria-live="polite"></div>
      ```
      Note: keep the plain `href` too so non-HTMX click still downloads the file directly.
    - Add a secondary line noting PDF: "PDF export is not yet available — download DOCX and
      convert locally." (matches D-08 / the 501 message).

    (b) Create templates/partials/export_result.html (HTMX success partial):
    ```html
    {# HTMX partial: export confirmation after successful DOCX generation (Phase 8) #}
    <div id="export-result" role="status" class="export-result">
        <h2>Export Complete</h2>
        <p>Your Work Description was generated successfully.</p>
        <p class="export-hash muted">SHA-256: <code>{{ export_hash }}</code></p>
        <a href="/export/{{ wd_id }}/docx" class="button button--primary">Download {{ filename }}</a>
    </div>
    ```

    (c) Edit templates/partials/jes_scores.html — activate the "Continue to Export" CTA.
    Replace the disabled anchor at lines 27-28:
    ```html
    <a href="/wizard/export?wd_id={{ wd_id }}" class="button button--secondary"
       aria-disabled="true" title="Available in Phase 8">Continue to Export</a>
    ```
    with the activated version:
    ```html
    <a href="/wizard/export?wd_id={{ wd_id }}" class="button button--primary">Continue to Export</a>
    ```
    Remove `aria-disabled="true"` and the `title="Available in Phase 8"` attribute entirely.
  </action>
  <verify>
    <automated>grep -q "/export/{{ wd_id }}/docx" templates/wizard/step_export.html && grep -q "export-result" templates/partials/export_result.html && ! grep -q "aria-disabled" templates/partials/jes_scores.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - templates/wizard/step_export.html contains `extends "base.html"`
    - templates/wizard/step_export.html contains `/export/{{ wd_id }}/docx`
    - templates/wizard/step_export.html contains `hx-target="#export-result"`
    - templates/partials/export_result.html contains `{{ export_hash }}`
    - templates/partials/export_result.html contains `Export Complete`
    - templates/partials/jes_scores.html contains `/wizard/export?wd_id={{ wd_id }}`
    - templates/partials/jes_scores.html does NOT contain `aria-disabled`
    - the grep verify command prints OK
  </acceptance_criteria>
  <done>Export wizard step + success partial created; JES "Continue to Export" CTA activated (no aria-disabled).</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS Layer 11 (Export) to main.css</name>
  <read_first>
    - app/static/css/main.css lines 1-90 (layer header comment block + CSS custom properties / color tokens like --color-text-muted, --color-error-text)
    - app/static/css/main.css last 40 lines (Layer 10 .jes-* classes — append after these; match formatting and token usage)
    - templates/wizard/step_export.html + templates/partials/export_result.html (class names to style: .export-result, .export-hash, .htmx-indicator usage, plus the error block)
  </read_first>
  <action>
    Append to the END of app/static/css/main.css a new section:
    ```css
    /* =========================================================================
       Layer 11: Export (Phase 8)
       .export-result, .export-hash, .export-errors, .export-error-card
       ========================================================================= */
    ```
    Then add rules (use existing CSS custom properties with fallbacks, matching the
    Layer 10 style):
    - `.export-result { margin-block-start: 1rem; padding: 1rem; border: 1px solid var(--color-border, #e5e7eb); border-radius: 0.5rem; }`
    - `.export-hash { font-size: 0.8rem; color: var(--color-text-muted, #6b7280); word-break: break-all; }`
    - `.export-errors { border: 1px solid var(--color-error-text, #dc2626); background: var(--color-error-bg, #fef2f2); padding: 0.75rem 1rem; border-radius: 0.5rem; margin-block: 1rem; }`
    - `.export-errors li { color: var(--color-error-text, #dc2626); }`
    - `.export-error-card { padding: 0.5rem 0.75rem; border-inline-start: 3px solid var(--color-error-text, #dc2626); margin-block-end: 0.5rem; }`
    - `.export-error-card--blocking { background: var(--color-error-bg, #fef2f2); }`
    Also update the layer-index comment near the top of the file (lines 4-15): add a line
    `*   11. Phase 8 Export components (.export-result, .export-hash, .export-errors)` to the
    layer index list so the header stays accurate.
  </action>
  <verify>
    <automated>grep -q "Layer 11" app/static/css/main.css && grep -q ".export-result" app/static/css/main.css && grep -q ".export-errors" app/static/css/main.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - app/static/css/main.css contains `Layer 11`
    - app/static/css/main.css contains `.export-result`
    - app/static/css/main.css contains `.export-hash`
    - app/static/css/main.css contains `.export-errors`
    - app/static/css/main.css header layer index contains `Phase 8 Export`
    - the grep verify command prints OK
  </acceptance_criteria>
  <done>CSS Layer 11 appended with export component styles; layer index comment updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies the export flow end-to-end</name>
  <what-built>
    Phase 8 complete: pre-export validation gate, DOCX export service rendering
    ProvenanceTag citations + version manifest + advisor-added markers, /export routes
    (docx + pdf 501), export wizard step, success partial, activated JES→Export CTA, and
    CSS layer 11.
  </what-built>
  <how-to-verify>
    1. Start the app: `uvicorn app.main:app --reload` (ensure Ollama is running and a
       WorkDescription has reached stage='jes_scored' via the wizard, or use a seeded wd_id).
    2. Open the JES scoring step in the browser; confirm the "Continue to Export" button is
       now enabled (primary style, clickable) and navigates to /wizard/export?wd_id=...
    3. On the export step, confirm: position summary text, the version manifest preview note,
       and a "Download DOCX" button render. Click Download DOCX.
    4. Confirm a .docx file downloads. Open it and verify:
       - Position Identification section shows title, number, group/level, supervisor, review date.
       - Key Activities lists duties; each duty shows a "Source: <id> (<version>)" citation line.
       - At least one advisor-added duty shows the literal text
         "advisor-added / not from authoritative source" with distinct (italic) styling (D-06).
       - JES Scoring section lists factors with level/points and source citations.
       - A "Source Document Version Manifest" table lists NOC, JES, TBS source rows with
         version + date (D-07).
    5. Visit `/export/<wd_id>/pdf` directly; confirm HTTP 501 with the message
       "PDF export is not yet available — download DOCX and convert locally."
    6. (Optional negative path) If you can produce a WD with an incomplete JES factor,
       confirm clicking Download DOCX surfaces a 422 naming the failed factor (D-01).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms DOCX downloads and opens
    - Human confirms citations render from sources (not hardcoded prose) — D-05
    - Human confirms advisor-added marker visible — D-06
    - Human confirms version manifest section present — D-07
    - Human confirms /pdf returns 501 — D-08
    - Human confirms "Continue to Export" CTA is enabled — Phase 7→8 handoff closed
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues (e.g., missing citation, broken CTA, manifest empty)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /wizard/export, /export/{wd_id}/docx | wd_id passed via query/path into templates and the service |
| template render → browser | export_hash and filename rendered into HTML |

## ASVS L1 Threat Register (STRIDE)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Cross-Site Scripting | export_result.html / step_export.html | mitigate | Jinja2 autoescaping is on by default (FastAPI Jinja2Templates); export_hash is a hex string and wd_id is a UUID — both rendered through autoescaped `{{ }}` (no `| safe`). |
| T-08-16 | Injection | wd_id in template hx-get URL | mitigate | wd_id is rendered into the URL via autoescaped `{{ }}`; server-side the route validates it through parameterized SQL load (08-03). |
| T-08-17 | Information Disclosure | export_hash display | accept | SHA-256 of the user's own document shown to the single local user; no cross-user exposure (single-user app). |
</threat_model>

<verification>
- grep confirms step_export.html, export_result.html created and jes_scores.html CTA activated (no aria-disabled)
- grep confirms CSS Layer 11 present
- `python -m pytest -q` — full suite green (no template-related import regressions)
- Human verify checkpoint approved
</verification>

<success_criteria>
- Export wizard step renders summary + manifest note + Download DOCX CTA (D-09)
- HTMX success partial confirms export with SHA-256 hash
- JES "Continue to Export" CTA activated and links to /wizard/export
- CSS Layer 11 styles export components
- Human confirms end-to-end: DOCX downloads with ProvenanceTag citations (D-05), advisor markers (D-06), version manifest (D-07); /pdf returns 501 (D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/08-export/08-04-SUMMARY.md`
</output>
