---
phase: 17-jes-scoring
plan: 04
type: execute
wave: 4
depends_on:
  - 17-03
files_modified: []
autonomous: false
requirements:
  - JES-01
  - JES-02
  - JES-03
  - JES-04
  - API-07

must_haves:
  truths:
    - "Full test suite passes (backend + frontend)"
    - "JES scorecard visible in Classification & Evaluation section after OG + level confirmed"
    - "Non-EC groups show single totals line with correct standard name"
    - "Failed-factor override input renders for degree=-1 factors"
    - "PATCH /api/wd/{id} persists jes_scores in work_descriptions table"
  artifacts:
    - path: "v2/backend/tests/test_jes_scoring.py"
      provides: "8 passing tests (4 unit + 4 integration)"
      contains: "8 passed"
    - path: "v2/frontend/src/document.test.jsx"
      provides: "2 passing ClassBlock render tests"
      contains: "2 passed"
  key_links:
    - from: "v2/frontend/src/app.jsx"
      to: "POST /api/jes/score"
      via: "og_level step commit chains JES fetch after PATCH resolves"
      pattern: "jes/score"
    - from: "v2/frontend/src/document.jsx"
      to: "record.jes_scores"
      via: "Section 4 reads record.jes_scores; ClassBlock renders rows"
      pattern: "jes_scores"
---

<objective>
Wave 4 UAT checkpoint: run the full test suite, then do a manual browser walkthrough confirming
the JES scorecard populates in the live preview after OG + level are confirmed.

Purpose: Validates JES-04 (visual render) and JES-02 (override UI) which cannot be automated.
Confirms no regressions in the 50 existing backend tests or 19+ existing frontend tests.

Output: Human UAT approval gate. Phase 17 can proceed to /gsd-verify-work after this passes.
</objective>

<execution_context>
@/home/charles/job_description_builder/.planning/phases/17-jes-scoring/execute-plan.md
@/home/charles/job_description_builder/.planning/phases/17-jes-scoring/summary.md
</execution_context>

<context>
@/home/charles/job_description_builder/.planning/PROJECT.md
@/home/charles/job_description_builder/.planning/phases/17-jes-scoring/17-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite green gate</name>
  <files></files>
  <read_first>
    - v2/backend/tests/test_jes_scoring.py — confirm test count (8 tests expected)
    - v2/frontend/src/document.test.jsx — confirm test count (2 tests expected)
  </read_first>
  <action>
Run the full test suite and confirm all tests pass. Fix any failures before proceeding to the
UAT checkpoint.

Backend:
```bash
cd /home/charles/job_description_builder/v2/backend && python3 -m pytest tests/ -q
```
Expected: 58 passed (50 existing + 8 JES) 0 failed.

Frontend:
```bash
cd /home/charles/job_description_builder/v2/frontend && npx vitest run
```
Expected: 21+ passed 0 failed (19 existing + 2 ClassBlock).

Build check:
```bash
cd /home/charles/job_description_builder/v2/frontend && npm run build
```
Expected: exits 0, bundle emits without TypeScript/ESLint errors.

If any test fails:
- Backend failures: read the test name, go to the relevant service/route file, fix the bug, re-run
- Frontend failures: run `npx vitest run --reporter=verbose` to get full error output, fix, re-run
- Do NOT skip or xfail tests to make the count pass
  </action>
  <verify>
    <automated>cd /home/charles/job_description_builder/v2/backend && python3 -m pytest tests/ -q 2>&1 | tail -3 && cd /home/charles/job_description_builder/v2/frontend && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - python3 -m pytest tests/ -q shows "N passed" with N >= 58 and "0 failed"
    - npx vitest run shows "N passed" with N >= 21 and "0 failed"
    - npm run build exits 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
JES scoring end-to-end:
- POST /api/jes/score returns 9-factor EC scorecard or non-EC single totals line
- app.jsx triggers JES fetch after og_level commits
- document.jsx Section 4 renders the scorecard rows once jes_scores populated
- Override input appears for factors with degree=-1 (failed sentinel)
  </what-built>
  <how-to-verify>
Start the dev environment:
```bash
cd /home/charles/job_description_builder/v2/backend && uvicorn app.main:app --port 8000 &
cd /home/charles/job_description_builder/v2/frontend && npm run dev
```
Open http://localhost:5173 in your browser.

Test A — EC JES scorecard render:
1. Complete the conversation through the og_level step (answer Role → Work Type → NOC confirm →
   OG confirm → OG level for an EC position, e.g. EC-05)
2. After confirming the level, the Classification & Evaluation section in the preview should
   show 9 per-factor rows: name / D{degree} / {points}
3. The totals row should show "Total — EC JES 2017" and a total points number (e.g. 720 pts)
4. Verify the badge at the top of the preview shows the EC-05 code

Test B — non-EC totals line:
1. Restart conversation (or open a fresh browser tab)
2. Follow the IT work type path through NOC confirm → OG confirm → select IT → og_level = 4
3. Classification & Evaluation section should show a single totals line:
   "Evaluated under the IT Job Evaluation Standard" with 480 pts

Test C — override UI for failed factor (if testable without real LLM failure):
1. Using curl or the browser dev tools, send a POST /api/jes/score with a mock/broken Ollama
   endpoint that causes factor scoring to fail, OR check that the override input renders when
   jes_scores contains a factor with degree=-1:
   In browser console: React devtools or manually set a factor degree to -1 in the network
   response to confirm the input renders.

Expected results:
- Test A: 9 rows visible in the preview, total pts displayed
- Test B: Single "Evaluated under..." line with correct pts
- Test C: Override number input visible for degree=-1 factor (if triggered)

Note any visual issues (wrong column alignment, missing rows, wrong pts values) as bugs.
  </how-to-verify>
  <resume-signal>Type "approved" if scorecard renders correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UAT | No new trust boundaries in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | — | UAT plan | accept | No production code changes; verification only |
</threat_model>

<verification>
Full suite: 58+ backend + 21+ frontend tests GREEN.
UAT: EC scorecard renders 9 rows with correct factor names and totals.
UAT: non-EC renders single totals line with standard name.
Build: npm run build exits 0.
</verification>

<success_criteria>
- python3 -m pytest v2/backend/tests/ -q shows 0 failed
- npx vitest run shows 0 failed
- npm run build exits 0
- Human UAT: EC JES scorecard renders in Classification & Evaluation section of live preview
- Human UAT: non-EC single totals line renders with correct standard name
</success_criteria>

<output>
After completion, create /home/charles/job_description_builder/.planning/phases/17-jes-scoring/17-04-SUMMARY.md
</output>
