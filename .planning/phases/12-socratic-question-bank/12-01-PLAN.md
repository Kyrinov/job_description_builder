---
phase: 12-socratic-question-bank
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - v2/backend/tests/test_question_bank.py
  - v2/backend/app/data/constants.py
autonomous: true
requirements: [QUES-01, QUES-02, QUES-03]

must_haves:
  truths:
    - "test_question_bank.py exists and all test functions are importable without error"
    - "constants.py exports KNOWN_JES_FACTORS as a frozenset of 9 exact strings"
    - "constants.py exports QUESTION_BANK as a list (may be empty stub at this wave)"
    - "Running pytest tests/test_question_bank.py produces structured RED failures (not ImportError)"
    - "Running pytest tests/test_constants.py (existing) still passes 8/8 green"
  artifacts:
    - path: "v2/backend/tests/test_question_bank.py"
      provides: "All 7 test functions defined for QUES-01, QUES-02, QUES-03"
      min_lines: 40
    - path: "v2/backend/app/data/constants.py"
      provides: "KNOWN_JES_FACTORS frozenset + QUESTION_BANK stub"
      contains: "KNOWN_JES_FACTORS"
  key_links:
    - from: "v2/backend/tests/test_question_bank.py"
      to: "v2/backend/app/data/constants.py"
      via: "from app.data.constants import QUESTION_BANK, OG_LEVELS, KNOWN_JES_FACTORS"
      pattern: "from app\\.data\\.constants import QUESTION_BANK"
---

<objective>
Write the complete test suite (RED) and the data structure stubs for the Socratic question bank.

Purpose: Establish the test contract before content is written, so Plan 02 has a clear pass/fail target. The KNOWN_JES_FACTORS frozenset is fully populated here (it drives test validation, not content). QUESTION_BANK is an empty list stub — tests that check content will be RED; import and schema tests will be structurally ready.

Output:
- v2/backend/tests/test_question_bank.py — 7 test functions, all referencing QUESTION_BANK, OG_LEVELS, KNOWN_JES_FACTORS
- v2/backend/app/data/constants.py — appended with KNOWN_JES_FACTORS (9 items) + QUESTION_BANK = [] stub
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/charles/job_description_builder/.planning/PROJECT.md
@/home/charles/job_description_builder/.planning/ROADMAP.md
@/home/charles/job_description_builder/.planning/STATE.md
</context>

<interfaces>
<!-- Exact current tail of v2/backend/app/data/constants.py after Phase 11. -->
<!-- Executor appends KNOWN_JES_FACTORS + QUESTION_BANK after this block. -->

From v2/backend/app/data/constants.py (lines 148–149, end of file):
```python
    "Lieutenant-Colonel / Commander": {
        "approx_annual_pay_cad": (166644, 177348),
        "approx_civilian_og_levels": ["IT-05"],
        "advisory": True,
        "note": "LCol/Cdr pay ($167k-$177k) falls in IT-05 mid-range ($133,249-$173,642).",
    },
}
```

Module docstring (lines 5–16) must be updated to add one-line entries for KNOWN_JES_FACTORS and QUESTION_BANK.

From v2/backend/tests/test_constants.py (import line):
```python
from app.data.constants import OG_LEVELS, CAF_RANK_OG_EQUIVALENCE
```
New test file uses:
```python
from app.data.constants import QUESTION_BANK, OG_LEVELS, KNOWN_JES_FACTORS
```

Section separator convention (every new constant block in constants.py):
```python
# ---------------------------------------------------------------------------
# CONSTANT_NAME
# One-sentence description.
# Source: provenance.
# Key: description. Value: description.
# ---------------------------------------------------------------------------
```
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Write test_question_bank.py (RED) + KNOWN_JES_FACTORS + QUESTION_BANK stub</name>
  <files>v2/backend/tests/test_question_bank.py, v2/backend/app/data/constants.py</files>

  <read_first>
    - v2/backend/app/data/constants.py — read the FULL file to see current structure, module docstring, section separator pattern, and end of CAF_RANK_OG_EQUIVALENCE block before appending
    - v2/backend/tests/test_constants.py — read in full for import pattern, docstring format, cross-reference assertion style, f-string failure message convention
    - v2/backend/pyproject.toml — confirm pytest testpaths and asyncio_mode settings
  </read_first>

  <behavior>
    Test 1 — test_question_bank_has_minimum_questions: assert len(QUESTION_BANK) >= 4 — FAILS (stub is [])
    Test 2 — test_every_entry_has_required_keys: iterates entries asserting REQUIRED_KEYS.issubset(entry.keys()) — PASSES vacuously (no entries)
    Test 3 — test_every_option_has_required_signal_keys: iterates entries → options → asserts REQUIRED_SIGNAL_KEYS.issubset(opt["signals"].keys()) — PASSES vacuously
    Test 4 — test_og_candidates_all_exist_in_og_levels: iterates entries → options → og_candidates → asserts code in OG_LEVELS — PASSES vacuously
    Test 5 — test_jes_factor_hints_all_known: iterates entries → options → jes_factor_hints → asserts hint in KNOWN_JES_FACTORS — PASSES vacuously
    Test 6 — test_no_og_codes_in_user_visible_text: iterates entries checking question/helper/labels — PASSES vacuously
    Test 7 — test_covers_minimum_four_groups: accumulates all og_candidates, asserts "EC", "AS", "IT", "FI" each present — FAILS (no entries, no candidates)
    (After Plan 02 populates content, tests 1 and 7 go GREEN; tests 2–6 go from vacuous-pass to real-pass)
  </behavior>

  <action>
**Step A — Append to v2/backend/app/data/constants.py:**

1. Update the module docstring (lines 5–16) to add two new lines before the closing `"""`:
   ```
   KNOWN_JES_FACTORS: frozenset of the 9 canonical EC JES factor name strings (use & not "and").
   QUESTION_BANK: list of Socratic work-type question entries; drives Phase 15 conversation flow.
   ```

2. After the closing `}` of CAF_RANK_OG_EQUIVALENCE (line 149), append:

```python


# ---------------------------------------------------------------------------
# KNOWN_JES_FACTORS
# Canonical JES factor names from EC JES 2017 (verified against EC_ELEMENTS in
# Job Description Builder/jd-builder/data.jsx). Used by test_question_bank.py
# to cross-reference jes_factor_hints in QUESTION_BANK signals.
# Key: N/A (frozenset). Value: exact factor name strings — use & not "and".
# ---------------------------------------------------------------------------

KNOWN_JES_FACTORS: frozenset[str] = frozenset({
    "Decision making",
    "Leadership & operational mgmt",
    "Communication",
    "Knowledge of specialized fields",
    "Contextual knowledge",
    "Research & analysis",
    "Physical effort",
    "Sensory effort",
    "Working conditions",
})


# ---------------------------------------------------------------------------
# QUESTION_BANK
# Socratic work-description questions that drive the work_type conversation phase.
# Each entry elicits a natural-language description from the manager; OG group
# is derived from accumulated signals, never directly selected by the user.
# QUES-02 constraint: OG codes must not appear in "question", "helper", or
# options[].label — only inside signals.og_candidates.
# Source: designed for v2.0; signals verified against OG_LEVELS and KNOWN_JES_FACTORS.
# Key: list index. Value: question entry dict with id, phase_slot, question,
#   helper, input_type, options (each option has id, label, signals).
# ---------------------------------------------------------------------------

QUESTION_BANK: list[dict] = [
    # Populated in Phase 12 Plan 02
]
```

**Step B — Create v2/backend/tests/test_question_bank.py:**

```python
"""
tests/test_question_bank.py — Unit tests for QUESTION_BANK in app/data/constants.py.

QUES-01 (5 tests): every entry has required keys; OG codes valid; JES hints valid;
    minimum question count; covers EC/AS/IT/FI.
QUES-02 (1 test): no OG code appears in user-visible text (question, helper, option label).
QUES-03 (1 test): all entries have phase_slot="work_type"; input_type is a known value.

Wave 0 (Plan 01): All tests are importable; tests 1 and 7 are RED (QUESTION_BANK is []).
    Tests 2–6 pass vacuously (no entries to iterate). Test file itself is GREEN structurally.
Wave 1 (Plan 02): Full QUESTION_BANK written; all 7 tests GREEN.
"""
from app.data.constants import QUESTION_BANK, OG_LEVELS, KNOWN_JES_FACTORS

REQUIRED_ENTRY_KEYS = {"id", "phase_slot", "question", "helper", "input_type", "options"}
REQUIRED_SIGNAL_KEYS = {"og_candidates", "jes_factor_hints", "teer_affinity"}
KNOWN_INPUT_TYPES = {"choices", "scale"}


def test_question_bank_has_minimum_questions():
    assert len(QUESTION_BANK) >= 4, \
        f"QUESTION_BANK must have at least 4 entries, found {len(QUESTION_BANK)}"


def test_every_entry_has_required_keys():
    for entry in QUESTION_BANK:
        missing = REQUIRED_ENTRY_KEYS - entry.keys()
        assert not missing, \
            f"Entry '{entry.get('id')}' missing required keys: {missing}"


def test_every_option_has_required_signal_keys():
    for entry in QUESTION_BANK:
        for opt in entry["options"]:
            missing = REQUIRED_SIGNAL_KEYS - opt["signals"].keys()
            assert not missing, \
                f"Option '{opt.get('id')}' in entry '{entry.get('id')}' missing signal keys: {missing}"


def test_og_candidates_all_exist_in_og_levels():
    all_og_codes = set(OG_LEVELS.keys())
    for entry in QUESTION_BANK:
        for opt in entry["options"]:
            for code in opt["signals"]["og_candidates"]:
                assert code in all_og_codes, \
                    f"Entry '{entry.get('id')}' option '{opt.get('id')}' references OG code '{code}' not in OG_LEVELS"


def test_jes_factor_hints_all_known():
    for entry in QUESTION_BANK:
        for opt in entry["options"]:
            for hint in opt["signals"]["jes_factor_hints"]:
                assert hint in KNOWN_JES_FACTORS, \
                    f"Entry '{entry.get('id')}' option '{opt.get('id')}' references unknown JES factor hint: '{hint}'"


def test_no_og_codes_in_user_visible_text():
    og_codes = set(OG_LEVELS.keys())
    for entry in QUESTION_BANK:
        for field in ("question", "helper"):
            for code in og_codes:
                assert code not in entry[field], \
                    f"OG code '{code}' found in user-visible field '{field}' of entry '{entry.get('id')}'"
        for opt in entry["options"]:
            for code in og_codes:
                assert code not in opt["label"], \
                    f"OG code '{code}' found in option label of entry '{entry.get('id')}'"


def test_covers_minimum_four_groups():
    all_candidates: set[str] = set()
    for entry in QUESTION_BANK:
        for opt in entry["options"]:
            all_candidates.update(opt["signals"]["og_candidates"])
    for required_group in ("EC", "AS", "IT", "FI"):
        assert required_group in all_candidates, \
            f"Required group '{required_group}' has no signal anywhere in QUESTION_BANK"


def test_all_entries_have_phase_slot_work_type():
    for entry in QUESTION_BANK:
        assert entry.get("phase_slot") == "work_type", \
            f"Entry '{entry.get('id')}' must have phase_slot='work_type', got '{entry.get('phase_slot')}'"


def test_all_entries_have_known_input_type():
    for entry in QUESTION_BANK:
        assert entry.get("input_type") in KNOWN_INPUT_TYPES, \
            f"Entry '{entry.get('id')}' has unknown input_type '{entry.get('input_type')}'; must be one of {KNOWN_INPUT_TYPES}"
```

Note: tests 1 and 7 (`test_question_bank_has_minimum_questions` and `test_covers_minimum_four_groups`) will be RED after this task because `QUESTION_BANK = []`. All other tests pass vacuously. This is the correct RED state for TDD.
  </action>

  <verify>
    <automated>cd /home/charles/job_description_builder/v2/backend && python -m pytest tests/test_question_bank.py -v 2>&1 | tail -20</automated>
    After this task, expected output: 2 FAILED (test_question_bank_has_minimum_questions, test_covers_minimum_four_groups), 7 PASSED.
    Also run: cd /home/charles/job_description_builder/v2/backend && python -m pytest tests/test_constants.py -v — must still be 8/8 green.
  </verify>

  <done>
    - v2/backend/tests/test_question_bank.py exists with 9 test functions (test_question_bank_has_minimum_questions, test_every_entry_has_required_keys, test_every_option_has_required_signal_keys, test_og_candidates_all_exist_in_og_levels, test_jes_factor_hints_all_known, test_no_og_codes_in_user_visible_text, test_covers_minimum_four_groups, test_all_entries_have_phase_slot_work_type, test_all_entries_have_known_input_type)
    - v2/backend/app/data/constants.py contains KNOWN_JES_FACTORS frozenset with exactly 9 members and QUESTION_BANK = [] stub
    - pytest tests/test_question_bank.py exits with 2 FAILED, 7 PASSED (not ImportError — structural RED is correct)
    - pytest tests/test_constants.py exits 8/8 green (no regressions)
    - grep confirms: "KNOWN_JES_FACTORS" in constants.py, "QUESTION_BANK" in constants.py
    - grep confirms: "from app.data.constants import QUESTION_BANK, OG_LEVELS, KNOWN_JES_FACTORS" in test_question_bank.py
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 12 is a pure static data constant + test. No user input, no network calls, no persistence layer. ASVS V2–V6 do not apply. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | constants.py QUESTION_BANK | accept | Static constant in source control; no runtime mutation path; content reviewed at commit |
</threat_model>

<verification>
1. `cd /home/charles/job_description_builder/v2/backend && python -m pytest tests/test_question_bank.py -v` — exits 2 FAILED / 7 PASSED (correct RED state)
2. `cd /home/charles/job_description_builder/v2/backend && python -m pytest tests/test_constants.py -v` — exits 8/8 green (no regressions)
3. `grep "KNOWN_JES_FACTORS" /home/charles/job_description_builder/v2/backend/app/data/constants.py` — returns a match
4. `grep "QUESTION_BANK" /home/charles/job_description_builder/v2/backend/app/data/constants.py` — returns a match
5. `grep "from app.data.constants import QUESTION_BANK" /home/charles/job_description_builder/v2/backend/tests/test_question_bank.py` — returns a match
</verification>

<success_criteria>
- test_question_bank.py exists with 9 test functions covering QUES-01, QUES-02, QUES-03
- constants.py exports KNOWN_JES_FACTORS (frozenset, 9 items) and QUESTION_BANK (empty list stub)
- pytest test_question_bank.py exits with exactly 2 FAILED, 7 PASSED — structural RED, not ImportError
- Existing 18/18 tests remain green (no regressions introduced)
</success_criteria>

<output>
After completion, create `/home/charles/job_description_builder/.planning/phases/12-socratic-question-bank/12-01-SUMMARY.md`
</output>
