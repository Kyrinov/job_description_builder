---
phase: 19-qualifications-amendments
plan: 04
type: execute
wave: 4
depends_on:
  - 19-03
files_modified: []
autonomous: false
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - AMEND-01
  - AMEND-02

must_haves:
  truths:
    - "Full backend + frontend test suite GREEN with 0 failures"
    - "Vite production build exits 0 with no errors"
    - "Human has confirmed qual prefill correct for EC and at least one other OG group"
    - "Human has confirmed amendment panel opens, saves, and shows gold dot in browser"
    - "Human has confirmed amendment note persists across page refresh"
  artifacts:
    - path: "v2/backend/tests/test_amendments.py"
      provides: "6 passing tests"
      contains: "manager_amendment"
    - path: "v2/backend/tests/test_quals.py"
      provides: "3 passing tests"
      contains: "QUAL_STANDARDS"
  key_links:
    - from: "UAT approval"
      to: "Phase 20 DOCX export"
      via: "Amendment notes stored in audit_log; Phase 20 docxtpl reads them for appendix"
      pattern: "manager_amendment"
---

<objective>
Full suite green gate + human UAT for Phase 19 requirements.

Purpose: Confirm all backend and frontend tests pass together, Vite build is clean, and human UAT validates the two user-facing capability clusters (OG-keyed qual defaults and amendment note panel).

Output: Verified test baseline + UAT sign-off. Phase 20 (Export) can proceed with confidence that amendment notes are correctly stored in audit_log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-qualifications-amendments/19-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite green gate + Vite build</name>
  <files></files>
  <read_first>
    - v2/backend/tests/test_quals.py (confirm 3 tests)
    - v2/backend/tests/test_amendments.py (confirm 6 tests, no skips)
  </read_first>
  <action>
Run the complete test suite and Vite production build. Report the results.

```bash
# Backend full suite
cd /home/charles/job_description_builder/v2/backend && python -m pytest tests/ -v 2>&1

# Frontend full suite
cd /home/charles/job_description_builder/v2/frontend && npx vitest run 2>&1

# Vite production build
cd /home/charles/job_description_builder/v2/frontend && npm run build 2>&1
```

If any test fails:
1. Read the failing test output carefully.
2. Identify whether the failure is in Phase 19 code (amendments.py, data.jsx, components.jsx, document.jsx, conversation.jsx) or in a prior phase's code.
3. Fix the root cause — do NOT skip or xfail a previously passing test.
4. Re-run until all suites pass.

Expected baseline:
- Backend: >= 73 passed (64 prior + 3 qual + 6 amendments), 0 failed
- Frontend: >= 31 passed, 0 failed
- Build: exits 0, bundle size reported

Report the actual counts in the summary.
  </action>
  <verify>
    <automated>cd /home/charles/job_description_builder/v2/backend && python -m pytest tests/ -q 2>&1 | tail -3 && cd /home/charles/job_description_builder/v2/frontend && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Backend output line matches: "X passed" where X >= 73, and "0 failed" or "failed" does not appear
    - Frontend output line matches: "X passed" where X >= 31, and "0 failed" or "X failed" does not appear
    - Vite build: "built in" appears in output (success); "error" does not appear in stderr
    - `grep "passed" backend_results` and `grep "passed" frontend_results` both return lines
  </acceptance_criteria>
  <done>All backend tests >= 73 GREEN; all frontend tests >= 31 GREEN; Vite build exits 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 19 delivers two capability clusters:

1. **OG-keyed Qualification Defaults (QUAL-01, QUAL-02, QUAL-03)**
   - The qualification step now pre-fills Education and Experience textareas with text matched to the confirmed OG group (EC, AS, IT, FI, or a generic fallback)
   - Blurring an empty field shows an inline error message "Education/Experience field is required."
   - The document preview Section 5 renders Education/Experience sub-labels in monospace caps
   - The "TBS Qualification Standard" provenance tag appears in the document footer once quals are visited

2. **Manager Amendment Notes (AMEND-01)**
   - In review state, every JD section header shows a small pencil icon button (.amend-btn)
   - Clicking the button opens an inline panel below the section header with a textarea, "Save note" and "Discard note" buttons, and a character count
   - Saving fires POST /api/wd/{id}/amendments and shows a toast "Note saved for {section name}."
   - A gold indicator dot appears on the section header when a note has been saved
   - Notes survive page refresh (hydrated via GET /api/wd/{id}/amendments)
   - The ReviewState checklist shows "N amendment note(s) attached" when notes exist
  </what-built>
  <how-to-verify>
Start the dev servers if not running:
```bash
cd /home/charles/job_description_builder/v2/backend && uvicorn app.main:app --port 8000 &
cd /home/charles/job_description_builder/v2/frontend && npm run dev &
```
Open http://localhost:5173 in a browser.

**Test 1 — EC Qualification Prefill (QUAL-01):**
1. Start or continue a work description where OG confirmation has been completed for an EC group
2. Navigate to the Qualifications step
3. Verify: Education textarea shows text containing "economics, sociology or statistics" or similar EC degree language
4. Verify: Experience textarea shows text containing "policy analysis, economic research"
5. Verify these are NOT the old EC-05 environmental text ("environmental program or policy analysis")

**Test 2 — Non-EC Prefill (QUAL-01):**
1. Start a fresh session and confirm an IT or AS group
2. Navigate to Qualifications step
3. Verify: Education textarea shows IT-specific text ("computer science, information technology") or AS-specific text ("secondary school diploma") — NOT EC text

**Test 3 — Inline Validation (QUAL-02):**
1. On the Qualifications step, clear the Education textarea completely
2. Click somewhere else (blur the field)
3. Verify: "Education field is required." error message appears below the textarea
4. Type something back into Education — verify the error disappears
5. Repeat for Experience field

**Test 4 — Document Preview Section 5 (QUAL-03):**
1. Submit the Qualifications step with non-empty text
2. In the document preview, scroll to "Essential Qualifications" section
3. Verify: "EDUCATION" and "EXPERIENCE" appear as uppercase monospace labels (Spline Sans Mono, small, ink-faint color)
4. Verify: "TBS Qualification Standard" src pill appears in the section header
5. Verify: "TBS Qualification Standard" appears in the provenance footer

**Test 5 — Amendment Panel (AMEND-01):**
1. Complete a full WD and enter review state (click "Finish & review" or reach the Review phase)
2. In the document preview, locate any section header (e.g., "Key Responsibilities")
3. Verify: A small pencil icon button (.amend-btn) is visible in the section header
4. Click the button — verify an inline panel opens below the header with a textarea and "Save note" / "Discard note" buttons
5. Type "This duty requires clarification." in the textarea
6. Click "Save note"
7. Verify: Toast "Note saved for Key Responsibilities." appears and fades
8. Verify: A gold dot indicator appears in the section header
9. Click the pencil button again — verify the panel reopens with the saved text pre-filled

**Test 6 — Amendment Persistence (AMEND-01):**
1. After saving a note in Test 5, reload the browser
2. Return to review state on the same WD
3. Verify: The gold dot is still visible on the section where you saved a note
4. Click the pencil button — verify the saved text is still pre-filled in the textarea

**Test 7 — ReviewState Checklist:**
1. In review state with at least 1 saved amendment note
2. Open the conversation pane (left side)
3. Verify: "1 amendment note attached" (or "N amendment notes attached") appears in the checklist items

Report any failures with a description of what was wrong.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT interaction | Human-only verification; no code changes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Information Disclosure | audit_log amendment data | accept | Single-user local app; no multi-user access control needed |
</threat_model>

<verification>
```bash
# Final confirmation commands
cd /home/charles/job_description_builder/v2/backend && python -m pytest tests/ -q
cd /home/charles/job_description_builder/v2/frontend && npx vitest run
cd /home/charles/job_description_builder/v2/frontend && npm run build

# Confirm amendment endpoint is reachable
curl -s http://localhost:8000/api/health
```
</verification>

<success_criteria>
- Backend: >= 73 tests passing, 0 failing
- Frontend: >= 31 tests passing, 0 failing
- Vite production build: exits 0
- Human UAT: all 7 test scenarios pass
- Phase 19 QUAL-01, QUAL-02, QUAL-03, AMEND-01, AMEND-02 all confirmed delivered
- Note: AMEND-02 (DOCX appendix) data is confirmed stored in audit_log; rendering deferred to Phase 20
</success_criteria>

<output>
After completion, create `.planning/phases/19-qualifications-amendments/19-04-SUMMARY.md`
</output>
