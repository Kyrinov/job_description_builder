---
phase: 25-accessible-template
plan: 03
type: execute
wave: 3
depends_on: ["25-01", "25-02"]
files_modified:
  - v2/backend/app/services/export_service.py
  - v2/backend/tests/test_export.py
  - v2/backend/app/templates/wd_template.docx
  - v2/backend/scripts/build_wd_template.py
autonomous: true
requirements: [ACC-02, ACC-03, ACC-04]
must_haves:
  truths:
    - "POST /api/wd/{id}/export/docx renders the Accessible-format template for any confirmed WD"
    - "Effort and Working Conditions sections populate from JES factor scores for groups whose JES defines those factors (EC, FB), and show '[To be completed by advisor]' for groups that do not (MT, level-description AS)"
    - "EC-scored WDs correctly populate Effort/Conditions even though the persisted jes_scores dict lacks a category key (category derived via factor_name lookup)"
    - "Every non-placeholder template variable resolves to a non-empty string for a fully-completed WD"
    - "The previous TBS template (wd_template.docx) and its build script are removed; the poster template is unchanged"
  artifacts:
    - path: "v2/backend/app/services/export_service.py"
      provides: "_factor_category_map helper + rewritten _build_wd_context + Accessible template path"
      contains: "_factor_category_map"
  key_links:
    - from: "v2/backend/app/services/export_service.py generate_wd_docx"
      to: "v2/backend/app/templates/wd_accessible_template.docx"
      via: "_resolve_template_path('wd_accessible_template.docx')"
      pattern: "wd_accessible_template\\.docx"
    - from: "_build_wd_context effort/conditions bucketing"
      to: "EC_JES_ELEMENTS + JES_FACTORS_BY_GROUP categories"
      via: "_factor_category_map().get(score['factor_name'])"
      pattern: "_factor_category_map"
---

<objective>
Rewrite `_build_wd_context()` in `export_service.py` to populate all Part 2 Accessible-format fields, switch the export template path to `wd_accessible_template.docx`, add a `_factor_category_map()` helper that derives factor categories from `constants.py` (never from the runtime score dict), update the existing test assertions, and retire the TBS template + its build script (ACC-02, ACC-03, ACC-04).

This turns the 6 RED tests from Plan 01 GREEN and keeps the existing 13 tests GREEN.

Purpose: Wire the Accessible template (built in Plan 02) into the live export path with correct JES-derived Effort/Working-Conditions data and a robust placeholder fallback.
Output: Rewritten export_service.py, updated tests, deleted TBS template + build script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-accessible-template/25-RESEARCH.md
@.planning/phases/25-accessible-template/25-PATTERNS.md

<interfaces>
<!-- Exact current state of export_service.py functions to modify. Executor MUST read the file first. -->

Current import (line 36): `from app.data.constants import NON_EC_STANDARD_NAMES`
Current template path call site (line 401): `template_path = _resolve_template_path("wd_template.docx")`
`_og_code_from(wd)` (line 136) — tolerant og_code reader; ALWAYS use it, never wd.confirmed_og.get directly.
`_build_organizational_context_text(wd)` (line 213) — REUSE verbatim for organizational_context_text.
`_build_v2_manifest(wd)` (around line 159) — REUSE verbatim for manifest.

Current _build_wd_context return dict (lines 267-290) — to be rewritten/extended. Keep the function name and signature `_build_wd_context(wd, amendments) -> dict`.

SW/ED routing-code resolution (replicate from jes_service.py score_jes_v2, lines ~199-217):
```python
sub_group = getattr(wd, "confirmed_sub_group", None)
routing_code = og_code
if og_code == "SW":
    routing_code = "SW-SCW" if sub_group == "SCW" else "SW-CHA"
elif og_code == "ED":
    if sub_group == "EDS": routing_code = "ED-EDS"
    elif sub_group == "LAT": routing_code = "ED-LAT"
    elif sub_group == "EST": routing_code = "ED-EST"
    else: routing_code = "ED-LAT"
```

Factor category map (NEW helper, from RESEARCH.md Code Examples):
```python
from app.data.constants import EC_JES_ELEMENTS, JES_FACTORS_BY_GROUP, NON_EC_STANDARD_NAMES

def _factor_category_map() -> dict[str, str]:
    mapping: dict[str, str] = {}
    for el in EC_JES_ELEMENTS:
        mapping[el["name"]] = el["category"]
    for group_factors in JES_FACTORS_BY_GROUP.values():
        for f in group_factors:
            mapping[f["name"]] = f.get("category", "")
    return mapping
```
Category values are exactly: "Effort", "Conditions", "Skill", "Responsibility".

Existing tests that must keep passing: test_export_wd_docx_returns_bytes, test_export_wd_docx_manifest, test_standard_names_import_from_constants (asserts the constants import shape). The new constants imports (EC_JES_ELEMENTS, JES_FACTORS_BY_GROUP) must be added to the SAME `from app.data.constants import` line/block so that test still passes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _factor_category_map helper + rewrite _build_wd_context for Accessible fields</name>
  <files>v2/backend/app/services/export_service.py</files>
  <read_first>
    - v2/backend/app/services/export_service.py (full — imports at line 36, _og_code_from at 136, _build_organizational_context_text at 213, _build_wd_context at 242, generate_wd_docx template path at 401)
    - v2/backend/app/services/jes_service.py (lines ~190-260 — routing_code resolution + how category is/isn't copied onto score dicts)
    - v2/backend/app/data/constants.py (lines 1366-1470 — EC_JES_ELEMENTS, JES_FACTORS_BY_GROUP categories)
    - v2/backend/tests/test_export.py (the RED tests from Plan 01 — the contract this task satisfies)
    - .planning/phases/25-accessible-template/25-PATTERNS.md (export_service.py Pattern Assignments, sections 1-4)
  </read_first>
  <behavior>
    - _factor_category_map() merges EC_JES_ELEMENTS + all JES_FACTORS_BY_GROUP into {factor_name: category}.
    - For an EC WD, effort_factors contains the scores whose factor_name maps to "Effort" ("Physical effort", "Sensory effort"); working_conditions_factors contains scores mapping to "Conditions" ("Working conditions"). effort_placeholder and wc_placeholder are "" (empty) when their list is non-empty.
    - For an MT WD (point-rating, no Effort/Conditions factors), effort_factors and working_conditions_factors are empty lists; effort_placeholder and wc_placeholder are "[To be completed by advisor]".
    - For a level-description WD (jes_scores == []), both lists empty, both placeholders "[To be completed by advisor]".
    - responsibilities_text: when any score maps to "Responsibility", join "factor_name: rationale" lines (rationale via score.get("rationale","") fallback); else "[To be completed by advisor]". Per RESEARCH.md every group has a Responsibility factor, so for empty jes_scores fall back to placeholder.
    - The 6 unmapped position fields (job_code, office_code, language_requirements, linguistic_profile, communications_requirements, security_requirements) resolve to "[To be completed by advisor]" (LOCKED DECISION 2).
    - supervisor_classification resolves to "" or "[To be completed by advisor]" — no WD source exists; use "[To be completed by advisor]" to match the placeholder convention.
    - client_service_results_text comes from record.get("client_service_results", "") with "[To be completed by advisor]" fallback when empty.
  </behavior>
  <action>
    In v2/backend/app/services/export_service.py:

    1. Extend the constants import at line 36 to: `from app.data.constants import EC_JES_ELEMENTS, JES_FACTORS_BY_GROUP, NON_EC_STANDARD_NAMES` (single import statement, alphabetized, so test_standard_names_import_from_constants still passes — it greps for "from app.data.constants import" and "NON_EC_STANDARD_NAMES").

    2. Add `_factor_category_map()` helper (verbatim from the interfaces block above) near _build_v2_manifest.

    3. Add a module-level constant `_ADVISOR_PLACEHOLDER = "[To be completed by advisor]"`.

    4. Rewrite `_build_wd_context(wd, amendments)` keeping the same signature. Compute:
       - og_code = _og_code_from(wd); resolve routing_code via the SW/ED block from the interfaces section.
       - cat_map = _factor_category_map()
       - scores = wd.jes_scores or []
       - effort_factors = [s for s in scores if cat_map.get(s.get("factor_name")) == "Effort"]
       - working_conditions_factors = [s for s in scores if cat_map.get(s.get("factor_name")) == "Conditions"]
       - responsibility_factors = [s for s in scores if cat_map.get(s.get("factor_name")) == "Responsibility"]
       - effort_placeholder = "" if effort_factors else _ADVISOR_PLACEHOLDER
       - wc_placeholder = "" if working_conditions_factors else _ADVISOR_PLACEHOLDER
       - responsibilities_text = "\n".join(f"{s.get('factor_name')}: {s.get('rationale','')}".rstrip(': ') for s in responsibility_factors) if responsibility_factors else _ADVISOR_PLACEHOLDER
       - client_service_results_text = (record.get("client_service_results") or "").strip() or _ADVISOR_PLACEHOLDER
    Return a dict with ALL keys from Plan 02's `required` set:
       position_number, position_title, og_level (use existing _og_level_str result), review_date, job_code=_ADVISOR_PLACEHOLDER, noc_code (from record.get("noc_code","") or _ADVISOR_PLACEHOLDER), department_name (record.get("department_name","") or _ADVISOR_PLACEHOLDER), geographic_location (record.get("location","") or _ADVISOR_PLACEHOLDER), org_component (record.get("branch","") or _ADVISOR_PLACEHOLDER), office_code=_ADVISOR_PLACEHOLDER, language_requirements=_ADVISOR_PLACEHOLDER, linguistic_profile=_ADVISOR_PLACEHOLDER, communications_requirements=_ADVISOR_PLACEHOLDER, security_requirements=_ADVISOR_PLACEHOLDER, supervisor_position_number (record.get("supervisor_position_number","") or _ADVISOR_PLACEHOLDER), supervisor_title (record.get("reports","") or _ADVISOR_PLACEHOLDER), supervisor_classification=_ADVISOR_PLACEHOLDER, organizational_context_text=_build_organizational_context_text(wd), client_service_results_text, duties (existing list-of-dict pattern from current _build_wd_context), education_text, experience_text (existing qualification fallback pattern), effort_factors, effort_placeholder, responsibilities_text, working_conditions_factors, wc_placeholder, manifest=_build_v2_manifest(wd), amendments.

    IMPORTANT: every value must be a non-empty string (or a list) — never None, never "" for a field the content-presence test checks. Use the `or _ADVISOR_PLACEHOLDER` idiom for every scalar that could be blank EXCEPT effort_placeholder/wc_placeholder which are intentionally "" when factors are present (those sections then render the factor table instead).

    Do NOT change generate_wd_docx's template path in this task — that is Task 2 (so the suite can be run incrementally).
  </action>
  <verify>
    <automated>cd /home/charles/job_description_builder && python -c "import ast; ast.parse(open('v2/backend/app/services/export_service.py').read()); print('parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'def _factor_category_map' v2/backend/app/services/export_service.py` returns one hit
    - `grep -n 'EC_JES_ELEMENTS' v2/backend/app/services/export_service.py` confirms import added on the constants line
    - `grep -n 'effort_factors\|working_conditions_factors\|effort_placeholder\|wc_placeholder\|responsibilities_text\|client_service_results_text' v2/backend/app/services/export_service.py` returns all 6 keys
    - `grep -c 'To be completed by advisor' v2/backend/app/services/export_service.py` returns >= 1 (the _ADVISOR_PLACEHOLDER constant)
    - `grep -n 'routing_code' v2/backend/app/services/export_service.py` confirms SW/ED routing replicated
    - `grep -n 'cat_map.get' v2/backend/app/services/export_service.py` confirms category derived via lookup, not `.get("category")` on the score dict
    - `grep -c 'score.get("category")\|s.get("category")\|s\["category"\]' v2/backend/app/services/export_service.py` returns 0 (must NOT trust runtime category key)
  </acceptance_criteria>
  <done>_factor_category_map + rewritten _build_wd_context produce all Accessible fields with correct category bucketing and placeholders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Switch export template path, update test assertions, retire TBS template</name>
  <files>v2/backend/app/services/export_service.py, v2/backend/tests/test_export.py, v2/backend/app/templates/wd_template.docx, v2/backend/scripts/build_wd_template.py</files>
  <read_first>
    - v2/backend/app/services/export_service.py (generate_wd_docx, line ~400-401 — the template path call site)
    - v2/backend/tests/test_export.py (existing assertions + the RED tests from Plan 01)
    - v2/backend/app/api/export.py (confirm it does NOT reference wd_template.docx directly — RESEARCH.md says it does not)
  </read_first>
  <behavior>
    - generate_wd_docx renders wd_accessible_template.docx (not wd_template.docx).
    - The 6 test_accessible_* tests from Plan 01 now PASS.
    - The original 13 tests still PASS.
    - `grep -rn "wd_template.docx"` across v2/backend (excluding .git) returns no hits in code (only possibly in historical comments — remove those too).
    - The poster template path and poster tests are unchanged.
  </behavior>
  <action>
    1. In v2/backend/app/services/export_service.py, change the WD export template path (line ~401):
       FROM `template_path = _resolve_template_path("wd_template.docx")`
       TO   `template_path = _resolve_template_path("wd_accessible_template.docx")`
       Also update the docstring on _build_wd_context (line 245) that says "variables declared in wd_template.docx" -> "wd_accessible_template.docx".

    2. Run `cd v2/backend && python -m pytest tests/test_export.py -x`. The 6 new accessible tests should now pass. If any fail, fix the context dict keys in Task 1 (factor_name strings, category bucketing, placeholder values) until GREEN. The existing structural tests (test_export_wd_docx_returns_bytes etc.) must stay green — they only assert MIME + bytes.

    3. If any existing test asserts TBS-specific text that no longer appears in the Accessible format, update that assertion to the Accessible-format equivalent (per RESEARCH.md, the existing tests assert only MIME/bytes/status so likely no change needed — but verify by running and reading any failure).

    4. Retire the TBS template per ACC-03: delete v2/backend/app/templates/wd_template.docx and v2/backend/scripts/build_wd_template.py using `git rm`. Confirm `grep -rn "wd_template.docx" v2/backend --include='*.py'` returns no remaining references (if export_service.py or any test still references it, fix). Do NOT touch poster_template.docx or build_poster_template.py.
  </action>
  <verify>
    <automated>cd /home/charles/job_description_builder/v2/backend && python -m pytest tests/test_export.py -q 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'wd_accessible_template.docx' v2/backend/app/services/export_service.py` confirms the path swap
    - `grep -rn 'wd_template.docx' v2/backend --include='*.py'` returns NO hits (TBS path fully retired)
    - `ls v2/backend/app/templates/wd_template.docx 2>&1` reports "No such file" (deleted)
    - `ls v2/backend/scripts/build_wd_template.py 2>&1` reports "No such file" (deleted)
    - `ls v2/backend/app/templates/poster_template.docx` succeeds (poster unchanged)
    - `cd v2/backend && python -m pytest tests/test_export.py -q` shows ALL tests passing (13 original + 6 new accessible = 19), 0 failed
    - `cd v2/backend && python -m pytest -q 2>&1 | tail -3` shows the full backend suite green (no regression in other test files)
  </acceptance_criteria>
  <done>Export route renders Accessible format; all 19 export tests green; full suite green; TBS template retired; poster untouched.</done>
</task>

</tasks>

<verification>
- `cd v2/backend && python -m pytest tests/test_export.py -q` — all 19 tests green (6 accessible turned GREEN)
- `cd v2/backend && python -m pytest` — full backend suite green, no regression
- `grep -rn 'wd_template.docx' v2/backend --include='*.py'` returns nothing
- poster_template.docx and build_poster_template.py untouched
</verification>

<success_criteria>
- ACC-02: Effort/Working-Conditions populate from JES factors (EC, FB) and fall back to "[To be completed by advisor]" (MT, AS) — verified by the 4 effort tests
- ACC-03: export route produces Accessible format; TBS template retired; poster unchanged; existing tests pass
- ACC-04: content-presence test green — no "{{" or stray "None" in rendered output
</success_criteria>

<output>
After completion, create `.planning/phases/25-accessible-template/25-03-SUMMARY.md`
</output>
